# ferray-random surface exclusions (Stage 4)
#
# RNG conformance is moment-based, not sample-exact (Stage 1 plan).
# Every public item in _surface.json that is not directly named in
# tests/conformance_random.rs is excluded here with a covered_by
# reference; each row carries a non-generic reason.

[[exclusion]]
path = "ferray_random::bitgen::mt19937::MT19937"
reason = "Struct declaration; constructors and methods are covered by the Generator moment tests in tests/conformance_random.rs (BitGenerator state advances on every sample)."
covered_by = "tests/conformance_random.rs"

[[exclusion]]
path = "ferray_random::bitgen::pcg64::Pcg64"
reason = "Struct declaration; constructors and methods are covered by the Generator moment tests in tests/conformance_random.rs (BitGenerator state advances on every sample)."
covered_by = "tests/conformance_random.rs"

[[exclusion]]
path = "ferray_random::bitgen::pcg64dxsm::Pcg64Dxsm"
reason = "Struct declaration; constructors and methods are covered by the Generator moment tests in tests/conformance_random.rs (BitGenerator state advances on every sample)."
covered_by = "tests/conformance_random.rs"

[[exclusion]]
path = "ferray_random::bitgen::philox::Philox"
reason = "Struct declaration; constructors and methods are covered by the Generator moment tests in tests/conformance_random.rs (BitGenerator state advances on every sample)."
covered_by = "tests/conformance_random.rs"

[[exclusion]]
path = "ferray_random::bitgen::seed_sequence::SeedSequence"
reason = "Struct declaration; constructors and methods are covered by the Generator moment tests in tests/conformance_random.rs (BitGenerator state advances on every sample)."
covered_by = "tests/conformance_random.rs"

[[exclusion]]
path = "ferray_random::bitgen::sfc64::Sfc64"
reason = "Struct declaration; constructors and methods are covered by the Generator moment tests in tests/conformance_random.rs (BitGenerator state advances on every sample)."
covered_by = "tests/conformance_random.rs"

[[exclusion]]
path = "ferray_random::bitgen::xoshiro256::Xoshiro256StarStar"
reason = "Struct declaration; constructors and methods are covered by the Generator moment tests in tests/conformance_random.rs (BitGenerator state advances on every sample)."
covered_by = "tests/conformance_random.rs"

[[exclusion]]
path = "ferray_random::distributions::discrete::Generator::geometric"
reason = "Method on Generator; the user-facing distribution surface is validated by the moment tests in tests/conformance_random.rs (mean/variance/range, not raw samples — Stage 1 plan)."
covered_by = "tests/conformance_random.rs"

[[exclusion]]
path = "ferray_random::distributions::discrete::Generator::hypergeometric"
reason = "Method on Generator; the user-facing distribution surface is validated by the moment tests in tests/conformance_random.rs (mean/variance/range, not raw samples — Stage 1 plan)."
covered_by = "tests/conformance_random.rs"

[[exclusion]]
path = "ferray_random::distributions::discrete::Generator::logseries"
reason = "Method on Generator; the user-facing distribution surface is validated by the moment tests in tests/conformance_random.rs (mean/variance/range, not raw samples — Stage 1 plan)."
covered_by = "tests/conformance_random.rs"

[[exclusion]]
path = "ferray_random::distributions::discrete::Generator::negative_binomial"
reason = "Method on Generator; the user-facing distribution surface is validated by the moment tests in tests/conformance_random.rs (mean/variance/range, not raw samples — Stage 1 plan)."
covered_by = "tests/conformance_random.rs"

[[exclusion]]
path = "ferray_random::distributions::discrete::Generator::poisson_array"
reason = "Method on Generator; the user-facing distribution surface is validated by the moment tests in tests/conformance_random.rs (mean/variance/range, not raw samples — Stage 1 plan)."
covered_by = "tests/conformance_random.rs"

[[exclusion]]
path = "ferray_random::distributions::discrete::Generator::zipf"
reason = "Method on Generator; the user-facing distribution surface is validated by the moment tests in tests/conformance_random.rs (mean/variance/range, not raw samples — Stage 1 plan)."
covered_by = "tests/conformance_random.rs"

[[exclusion]]
path = "ferray_random::distributions::gamma::Generator::chisquare"
reason = "Method on Generator; the user-facing distribution surface is validated by the moment tests in tests/conformance_random.rs (mean/variance/range, not raw samples — Stage 1 plan)."
covered_by = "tests/conformance_random.rs"

[[exclusion]]
path = "ferray_random::distributions::gamma::Generator::f"
reason = "Method on Generator; the user-facing distribution surface is validated by the moment tests in tests/conformance_random.rs (mean/variance/range, not raw samples — Stage 1 plan)."
covered_by = "tests/conformance_random.rs"

[[exclusion]]
path = "ferray_random::distributions::gamma::Generator::noncentral_chisquare"
reason = "Method on Generator; the user-facing distribution surface is validated by the moment tests in tests/conformance_random.rs (mean/variance/range, not raw samples — Stage 1 plan)."
covered_by = "tests/conformance_random.rs"

[[exclusion]]
path = "ferray_random::distributions::gamma::Generator::noncentral_f"
reason = "Method on Generator; the user-facing distribution surface is validated by the moment tests in tests/conformance_random.rs (mean/variance/range, not raw samples — Stage 1 plan)."
covered_by = "tests/conformance_random.rs"

[[exclusion]]
path = "ferray_random::distributions::gamma::Generator::standard_gamma"
reason = "Method on Generator; the user-facing distribution surface is validated by the moment tests in tests/conformance_random.rs (mean/variance/range, not raw samples — Stage 1 plan)."
covered_by = "tests/conformance_random.rs"

[[exclusion]]
path = "ferray_random::distributions::gamma::Generator::standard_t"
reason = "Method on Generator; the user-facing distribution surface is validated by the moment tests in tests/conformance_random.rs (mean/variance/range, not raw samples — Stage 1 plan)."
covered_by = "tests/conformance_random.rs"

[[exclusion]]
path = "ferray_random::distributions::gamma::Generator::student_t"
reason = "Method on Generator; the user-facing distribution surface is validated by the moment tests in tests/conformance_random.rs (mean/variance/range, not raw samples — Stage 1 plan)."
covered_by = "tests/conformance_random.rs"

[[exclusion]]
path = "ferray_random::distributions::misc_continuous::Generator::gumbel"
reason = "Method on Generator; the user-facing distribution surface is validated by the moment tests in tests/conformance_random.rs (mean/variance/range, not raw samples — Stage 1 plan)."
covered_by = "tests/conformance_random.rs"

[[exclusion]]
path = "ferray_random::distributions::misc_continuous::Generator::laplace"
reason = "Method on Generator; the user-facing distribution surface is validated by the moment tests in tests/conformance_random.rs (mean/variance/range, not raw samples — Stage 1 plan)."
covered_by = "tests/conformance_random.rs"

[[exclusion]]
path = "ferray_random::distributions::misc_continuous::Generator::logistic"
reason = "Method on Generator; the user-facing distribution surface is validated by the moment tests in tests/conformance_random.rs (mean/variance/range, not raw samples — Stage 1 plan)."
covered_by = "tests/conformance_random.rs"

[[exclusion]]
path = "ferray_random::distributions::misc_continuous::Generator::pareto"
reason = "Method on Generator; the user-facing distribution surface is validated by the moment tests in tests/conformance_random.rs (mean/variance/range, not raw samples — Stage 1 plan)."
covered_by = "tests/conformance_random.rs"

[[exclusion]]
path = "ferray_random::distributions::misc_continuous::Generator::power"
reason = "Method on Generator; the user-facing distribution surface is validated by the moment tests in tests/conformance_random.rs (mean/variance/range, not raw samples — Stage 1 plan)."
covered_by = "tests/conformance_random.rs"

[[exclusion]]
path = "ferray_random::distributions::misc_continuous::Generator::rayleigh"
reason = "Method on Generator; the user-facing distribution surface is validated by the moment tests in tests/conformance_random.rs (mean/variance/range, not raw samples — Stage 1 plan)."
covered_by = "tests/conformance_random.rs"

[[exclusion]]
path = "ferray_random::distributions::misc_continuous::Generator::standard_cauchy"
reason = "Method on Generator; the user-facing distribution surface is validated by the moment tests in tests/conformance_random.rs (mean/variance/range, not raw samples — Stage 1 plan)."
covered_by = "tests/conformance_random.rs"

[[exclusion]]
path = "ferray_random::distributions::misc_continuous::Generator::triangular"
reason = "Method on Generator; the user-facing distribution surface is validated by the moment tests in tests/conformance_random.rs (mean/variance/range, not raw samples — Stage 1 plan)."
covered_by = "tests/conformance_random.rs"

[[exclusion]]
path = "ferray_random::distributions::misc_continuous::Generator::vonmises"
reason = "Method on Generator; the user-facing distribution surface is validated by the moment tests in tests/conformance_random.rs (mean/variance/range, not raw samples — Stage 1 plan)."
covered_by = "tests/conformance_random.rs"

[[exclusion]]
path = "ferray_random::distributions::misc_continuous::Generator::wald"
reason = "Method on Generator; the user-facing distribution surface is validated by the moment tests in tests/conformance_random.rs (mean/variance/range, not raw samples — Stage 1 plan)."
covered_by = "tests/conformance_random.rs"

[[exclusion]]
path = "ferray_random::distributions::misc_continuous::Generator::weibull"
reason = "Method on Generator; the user-facing distribution surface is validated by the moment tests in tests/conformance_random.rs (mean/variance/range, not raw samples — Stage 1 plan)."
covered_by = "tests/conformance_random.rs"

[[exclusion]]
path = "ferray_random::distributions::multivariate::Generator::dirichlet"
reason = "Method on Generator; the user-facing distribution surface is validated by the moment tests in tests/conformance_random.rs (mean/variance/range, not raw samples — Stage 1 plan)."
covered_by = "tests/conformance_random.rs"

[[exclusion]]
path = "ferray_random::distributions::multivariate::Generator::multinomial"
reason = "Method on Generator; the user-facing distribution surface is validated by the moment tests in tests/conformance_random.rs (mean/variance/range, not raw samples — Stage 1 plan)."
covered_by = "tests/conformance_random.rs"

[[exclusion]]
path = "ferray_random::distributions::multivariate::Generator::multivariate_hypergeometric"
reason = "Method on Generator; the user-facing distribution surface is validated by the moment tests in tests/conformance_random.rs (mean/variance/range, not raw samples — Stage 1 plan)."
covered_by = "tests/conformance_random.rs"

[[exclusion]]
path = "ferray_random::distributions::multivariate::Generator::multivariate_normal"
reason = "Method on Generator; the user-facing distribution surface is validated by the moment tests in tests/conformance_random.rs (mean/variance/range, not raw samples — Stage 1 plan)."
covered_by = "tests/conformance_random.rs"

[[exclusion]]
path = "ferray_random::distributions::multivariate::Generator::multivariate_normal_array"
reason = "Method on Generator; the user-facing distribution surface is validated by the moment tests in tests/conformance_random.rs (mean/variance/range, not raw samples — Stage 1 plan)."
covered_by = "tests/conformance_random.rs"

[[exclusion]]
path = "ferray_random::distributions::ziggurat::standard_normal_ziggurat"
reason = "Internal ziggurat sampler used by Generator::standard_normal; correctness is observed via the standard_normal moment test in tests/conformance_random.rs."
covered_by = "tests/conformance_random.rs"

[[exclusion]]
path = "ferray_random::distributions::ziggurat::standard_normal_ziggurat_f32"
reason = "Internal ziggurat sampler used by Generator::standard_normal; correctness is observed via the standard_normal moment test in tests/conformance_random.rs."
covered_by = "tests/conformance_random.rs"

[[exclusion]]
path = "ferray_random::permutations::Generator::choice_dyn"
reason = "Method on Generator; the user-facing distribution surface is validated by the moment tests in tests/conformance_random.rs (mean/variance/range, not raw samples — Stage 1 plan)."
covered_by = "tests/conformance_random.rs"

[[exclusion]]
path = "ferray_random::permutations::Generator::permutation_range"
reason = "Method on Generator; the user-facing distribution surface is validated by the moment tests in tests/conformance_random.rs (mean/variance/range, not raw samples — Stage 1 plan)."
covered_by = "tests/conformance_random.rs"

[[exclusion]]
path = "ferray_random::permutations::Generator::permuted"
reason = "Method on Generator; the user-facing distribution surface is validated by the moment tests in tests/conformance_random.rs (mean/variance/range, not raw samples — Stage 1 plan)."
covered_by = "tests/conformance_random.rs"

[[exclusion]]
path = "ferray_random::permutations::Generator::permuted_dyn"
reason = "Method on Generator; the user-facing distribution surface is validated by the moment tests in tests/conformance_random.rs (mean/variance/range, not raw samples — Stage 1 plan)."
covered_by = "tests/conformance_random.rs"

[[exclusion]]
path = "ferray_random::permutations::Generator::shuffle_dyn"
reason = "Method on Generator; the user-facing distribution surface is validated by the moment tests in tests/conformance_random.rs (mean/variance/range, not raw samples — Stage 1 plan)."
covered_by = "tests/conformance_random.rs"

