# Surface exclusions for ferray-ufunc, generated by
# scripts/gen_ufunc_exclusions.py. Each entry covers a public
# item that is not (or not yet) directly mentioned by a
# conformance test under `tests/conformance_*.rs`. Categories:
#   - Inner canonical paths (`ops::<mod>::<fn>`) whose re-export
#     is covered: inner is implicitly verified by the same test.
#   - Internal modules (`dispatch::`, `helpers::`, `kernels::`,
#     etc.): not part of the user-facing API contract.
#   - User-facing items without a fixture: tracked under
#     umbrella issue #751.


[[exclusion]]
path = "ferray_ufunc::BitwiseCount"
reason = "User-facing re-export `ferray_ufunc::BitwiseCount` lacks a dedicated NumPy/scipy fixture in `fixtures/ufunc/`. Fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::BitwiseOps"
reason = "User-facing re-export `ferray_ufunc::BitwiseOps` lacks a dedicated NumPy/scipy fixture in `fixtures/ufunc/`. Fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::ConvolveMode"
reason = "User-facing re-export `ferray_ufunc::ConvolveMode` lacks a dedicated NumPy/scipy fixture in `fixtures/ufunc/`. Fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::Logical"
reason = "User-facing re-export `ferray_ufunc::Logical` lacks a dedicated NumPy/scipy fixture in `fixtures/ufunc/`. Fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::ShiftOps"
reason = "User-facing re-export `ferray_ufunc::ShiftOps` lacks a dedicated NumPy/scipy fixture in `fixtures/ufunc/`. Fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::Ufunc"
reason = "User-facing re-export `ferray_ufunc::Ufunc` lacks a dedicated NumPy/scipy fixture in `fixtures/ufunc/`. Fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::absolute_f16"
reason = "User-facing re-export `ferray_ufunc::absolute_f16` aliases a function family whose representative is exercised by the conformance test cited in covered_by. Dedicated fixture coverage for this alias is tracked under issue #751."
covered_by = "tests/conformance_arithmetic.rs::absolute_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::absolute_into"
reason = "User-facing re-export `ferray_ufunc::absolute_into` aliases a function family whose representative is exercised by the conformance test cited in covered_by. Dedicated fixture coverage for this alias is tracked under issue #751."
covered_by = "tests/conformance_arithmetic.rs::absolute_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::accumulate_axis"
reason = "User-facing re-export `ferray_ufunc::accumulate_axis` lacks a dedicated NumPy/scipy fixture in `fixtures/ufunc/`. Fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::acos_complex"
reason = "User-facing re-export `ferray_ufunc::acos_complex` lacks a dedicated NumPy/scipy fixture in `fixtures/ufunc/`. Fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::acosh_complex"
reason = "User-facing re-export `ferray_ufunc::acosh_complex` lacks a dedicated NumPy/scipy fixture in `fixtures/ufunc/`. Fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::add_accumulate"
reason = "User-facing re-export `ferray_ufunc::add_accumulate` aliases a function family whose representative is exercised by the conformance test cited in covered_by. Dedicated fixture coverage for this alias is tracked under issue #751."
covered_by = "tests/conformance_arithmetic.rs::add_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::add_broadcast"
reason = "User-facing re-export `ferray_ufunc::add_broadcast` aliases a function family whose representative is exercised by the conformance test cited in covered_by. Dedicated fixture coverage for this alias is tracked under issue #751."
covered_by = "tests/conformance_arithmetic.rs::add_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::add_datetime_timedelta"
reason = "User-facing re-export `ferray_ufunc::add_datetime_timedelta` lacks a dedicated NumPy/scipy fixture in `fixtures/ufunc/`. Fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::add_datetime_timedelta_promoted"
reason = "User-facing re-export `ferray_ufunc::add_datetime_timedelta_promoted` lacks a dedicated NumPy/scipy fixture in `fixtures/ufunc/`. Fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::add_f16"
reason = "User-facing re-export `ferray_ufunc::add_f16` aliases a function family whose representative is exercised by the conformance test cited in covered_by. Dedicated fixture coverage for this alias is tracked under issue #751."
covered_by = "tests/conformance_arithmetic.rs::add_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::add_into"
reason = "User-facing re-export `ferray_ufunc::add_into` aliases a function family whose representative is exercised by the conformance test cited in covered_by. Dedicated fixture coverage for this alias is tracked under issue #751."
covered_by = "tests/conformance_arithmetic.rs::add_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::add_promoted"
reason = "User-facing re-export `ferray_ufunc::add_promoted` aliases a function family whose representative is exercised by the conformance test cited in covered_by. Dedicated fixture coverage for this alias is tracked under issue #751."
covered_by = "tests/conformance_arithmetic.rs::add_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::add_reduce"
reason = "User-facing re-export `ferray_ufunc::add_reduce` aliases a function family whose representative is exercised by the conformance test cited in covered_by. Dedicated fixture coverage for this alias is tracked under issue #751."
covered_by = "tests/conformance_arithmetic.rs::add_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::add_reduce_all"
reason = "User-facing re-export `ferray_ufunc::add_reduce_all` aliases a function family whose representative is exercised by the conformance test cited in covered_by. Dedicated fixture coverage for this alias is tracked under issue #751."
covered_by = "tests/conformance_arithmetic.rs::add_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::add_reduce_axes"
reason = "User-facing re-export `ferray_ufunc::add_reduce_axes` aliases a function family whose representative is exercised by the conformance test cited in covered_by. Dedicated fixture coverage for this alias is tracked under issue #751."
covered_by = "tests/conformance_arithmetic.rs::add_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::add_reduce_keepdims"
reason = "User-facing re-export `ferray_ufunc::add_reduce_keepdims` aliases a function family whose representative is exercised by the conformance test cited in covered_by. Dedicated fixture coverage for this alias is tracked under issue #751."
covered_by = "tests/conformance_arithmetic.rs::add_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::add_timedelta"
reason = "User-facing re-export `ferray_ufunc::add_timedelta` lacks a dedicated NumPy/scipy fixture in `fixtures/ufunc/`. Fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::add_timedelta_promoted"
reason = "User-facing re-export `ferray_ufunc::add_timedelta_promoted` lacks a dedicated NumPy/scipy fixture in `fixtures/ufunc/`. Fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::add_ufunc"
reason = "User-facing re-export `ferray_ufunc::add_ufunc` lacks a dedicated NumPy/scipy fixture in `fixtures/ufunc/`. Fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::all"
reason = "User-facing re-export `ferray_ufunc::all` lacks a dedicated NumPy/scipy fixture in `fixtures/ufunc/`. Fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::all_axis"
reason = "User-facing re-export `ferray_ufunc::all_axis` lacks a dedicated NumPy/scipy fixture in `fixtures/ufunc/`. Fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::allclose"
reason = "User-facing re-export `ferray_ufunc::allclose` lacks a dedicated NumPy/scipy fixture in `fixtures/ufunc/`. Fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::angle"
reason = "User-facing re-export `ferray_ufunc::angle` lacks a dedicated NumPy/scipy fixture in `fixtures/ufunc/`. Fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::any"
reason = "User-facing re-export `ferray_ufunc::any` lacks a dedicated NumPy/scipy fixture in `fixtures/ufunc/`. Fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::any_axis"
reason = "User-facing re-export `ferray_ufunc::any_axis` lacks a dedicated NumPy/scipy fixture in `fixtures/ufunc/`. Fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::arccos_f16"
reason = "User-facing re-export `ferray_ufunc::arccos_f16` aliases a function family whose representative is exercised by the conformance test cited in covered_by. Dedicated fixture coverage for this alias is tracked under issue #751."
covered_by = "tests/conformance_trig.rs::arccos_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::arccosh_f16"
reason = "User-facing re-export `ferray_ufunc::arccosh_f16` aliases a function family whose representative is exercised by the conformance test cited in covered_by. Dedicated fixture coverage for this alias is tracked under issue #751."
covered_by = "tests/conformance_trig.rs::arccosh_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::arcsin_f16"
reason = "User-facing re-export `ferray_ufunc::arcsin_f16` aliases a function family whose representative is exercised by the conformance test cited in covered_by. Dedicated fixture coverage for this alias is tracked under issue #751."
covered_by = "tests/conformance_trig.rs::arcsin_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::arcsinh_f16"
reason = "User-facing re-export `ferray_ufunc::arcsinh_f16` aliases a function family whose representative is exercised by the conformance test cited in covered_by. Dedicated fixture coverage for this alias is tracked under issue #751."
covered_by = "tests/conformance_trig.rs::arcsinh_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::arctan2_f16"
reason = "User-facing re-export `ferray_ufunc::arctan2_f16` aliases a function family whose representative is exercised by the conformance test cited in covered_by. Dedicated fixture coverage for this alias is tracked under issue #751."
covered_by = "tests/conformance_trig.rs::arctan2_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::arctan_f16"
reason = "User-facing re-export `ferray_ufunc::arctan_f16` aliases a function family whose representative is exercised by the conformance test cited in covered_by. Dedicated fixture coverage for this alias is tracked under issue #751."
covered_by = "tests/conformance_trig.rs::arctan_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::arctanh_f16"
reason = "User-facing re-export `ferray_ufunc::arctanh_f16` aliases a function family whose representative is exercised by the conformance test cited in covered_by. Dedicated fixture coverage for this alias is tracked under issue #751."
covered_by = "tests/conformance_trig.rs::arctanh_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::around"
reason = "User-facing re-export `ferray_ufunc::around` lacks a dedicated NumPy/scipy fixture in `fixtures/ufunc/`. Fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::array_add"
reason = "User-facing re-export `ferray_ufunc::array_add` lacks a dedicated NumPy/scipy fixture in `fixtures/ufunc/`. Fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::array_bitand"
reason = "User-facing re-export `ferray_ufunc::array_bitand` lacks a dedicated NumPy/scipy fixture in `fixtures/ufunc/`. Fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::array_bitnot"
reason = "User-facing re-export `ferray_ufunc::array_bitnot` lacks a dedicated NumPy/scipy fixture in `fixtures/ufunc/`. Fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::array_bitor"
reason = "User-facing re-export `ferray_ufunc::array_bitor` lacks a dedicated NumPy/scipy fixture in `fixtures/ufunc/`. Fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::array_bitxor"
reason = "User-facing re-export `ferray_ufunc::array_bitxor` lacks a dedicated NumPy/scipy fixture in `fixtures/ufunc/`. Fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::array_div"
reason = "User-facing re-export `ferray_ufunc::array_div` lacks a dedicated NumPy/scipy fixture in `fixtures/ufunc/`. Fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::array_equal"
reason = "User-facing re-export `ferray_ufunc::array_equal` lacks a dedicated NumPy/scipy fixture in `fixtures/ufunc/`. Fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::array_equiv"
reason = "User-facing re-export `ferray_ufunc::array_equiv` lacks a dedicated NumPy/scipy fixture in `fixtures/ufunc/`. Fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::array_mul"
reason = "User-facing re-export `ferray_ufunc::array_mul` lacks a dedicated NumPy/scipy fixture in `fixtures/ufunc/`. Fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::array_neg"
reason = "User-facing re-export `ferray_ufunc::array_neg` lacks a dedicated NumPy/scipy fixture in `fixtures/ufunc/`. Fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::array_rem"
reason = "User-facing re-export `ferray_ufunc::array_rem` lacks a dedicated NumPy/scipy fixture in `fixtures/ufunc/`. Fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::array_shl"
reason = "User-facing re-export `ferray_ufunc::array_shl` lacks a dedicated NumPy/scipy fixture in `fixtures/ufunc/`. Fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::array_shr"
reason = "User-facing re-export `ferray_ufunc::array_shr` lacks a dedicated NumPy/scipy fixture in `fixtures/ufunc/`. Fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::array_sub"
reason = "User-facing re-export `ferray_ufunc::array_sub` lacks a dedicated NumPy/scipy fixture in `fixtures/ufunc/`. Fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::asin_complex"
reason = "User-facing re-export `ferray_ufunc::asin_complex` lacks a dedicated NumPy/scipy fixture in `fixtures/ufunc/`. Fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::asinh_complex"
reason = "User-facing re-export `ferray_ufunc::asinh_complex` lacks a dedicated NumPy/scipy fixture in `fixtures/ufunc/`. Fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::at"
reason = "User-facing re-export `ferray_ufunc::at` lacks a dedicated NumPy/scipy fixture in `fixtures/ufunc/`. Fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::atan_complex"
reason = "User-facing re-export `ferray_ufunc::atan_complex` lacks a dedicated NumPy/scipy fixture in `fixtures/ufunc/`. Fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::atanh_complex"
reason = "User-facing re-export `ferray_ufunc::atanh_complex` lacks a dedicated NumPy/scipy fixture in `fixtures/ufunc/`. Fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::bitwise_and"
reason = "User-facing re-export `ferray_ufunc::bitwise_and` lacks a dedicated NumPy/scipy fixture in `fixtures/ufunc/`. Fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::bitwise_count"
reason = "User-facing re-export `ferray_ufunc::bitwise_count` lacks a dedicated NumPy/scipy fixture in `fixtures/ufunc/`. Fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::bitwise_not"
reason = "User-facing re-export `ferray_ufunc::bitwise_not` lacks a dedicated NumPy/scipy fixture in `fixtures/ufunc/`. Fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::bitwise_or"
reason = "User-facing re-export `ferray_ufunc::bitwise_or` lacks a dedicated NumPy/scipy fixture in `fixtures/ufunc/`. Fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::bitwise_xor"
reason = "User-facing re-export `ferray_ufunc::bitwise_xor` lacks a dedicated NumPy/scipy fixture in `fixtures/ufunc/`. Fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::cbrt_f16"
reason = "User-facing re-export `ferray_ufunc::cbrt_f16` aliases a function family whose representative is exercised by the conformance test cited in covered_by. Dedicated fixture coverage for this alias is tracked under issue #751."
covered_by = "tests/conformance_arithmetic.rs::cbrt_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::ceil_f16"
reason = "User-facing re-export `ferray_ufunc::ceil_f16` aliases a function family whose representative is exercised by the conformance test cited in covered_by. Dedicated fixture coverage for this alias is tracked under issue #751."
covered_by = "tests/conformance_floatintrinsic.rs::ceil_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::clip_f16"
reason = "User-facing re-export `ferray_ufunc::clip_f16` aliases a function family whose representative is exercised by the conformance test cited in covered_by. Dedicated fixture coverage for this alias is tracked under issue #751."
covered_by = "tests/conformance_floatintrinsic.rs::clip_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::clip_ord"
reason = "User-facing re-export `ferray_ufunc::clip_ord` lacks a dedicated NumPy/scipy fixture in `fixtures/ufunc/`. Fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::conj"
reason = "User-facing re-export `ferray_ufunc::conj` lacks a dedicated NumPy/scipy fixture in `fixtures/ufunc/`. Fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::conjugate"
reason = "User-facing re-export `ferray_ufunc::conjugate` lacks a dedicated NumPy/scipy fixture in `fixtures/ufunc/`. Fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::convolve"
reason = "User-facing re-export `ferray_ufunc::convolve` lacks a dedicated NumPy/scipy fixture in `fixtures/ufunc/`. Fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::copysign"
reason = "User-facing re-export `ferray_ufunc::copysign` lacks a dedicated NumPy/scipy fixture in `fixtures/ufunc/`. Fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::cos_complex"
reason = "User-facing re-export `ferray_ufunc::cos_complex` lacks a dedicated NumPy/scipy fixture in `fixtures/ufunc/`. Fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::cos_f16"
reason = "User-facing re-export `ferray_ufunc::cos_f16` aliases a function family whose representative is exercised by the conformance test cited in covered_by. Dedicated fixture coverage for this alias is tracked under issue #751."
covered_by = "tests/conformance_trig.rs::cos_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::cos_into"
reason = "User-facing re-export `ferray_ufunc::cos_into` aliases a function family whose representative is exercised by the conformance test cited in covered_by. Dedicated fixture coverage for this alias is tracked under issue #751."
covered_by = "tests/conformance_trig.rs::cos_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::cosh_complex"
reason = "User-facing re-export `ferray_ufunc::cosh_complex` lacks a dedicated NumPy/scipy fixture in `fixtures/ufunc/`. Fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::cosh_f16"
reason = "User-facing re-export `ferray_ufunc::cosh_f16` aliases a function family whose representative is exercised by the conformance test cited in covered_by. Dedicated fixture coverage for this alias is tracked under issue #751."
covered_by = "tests/conformance_trig.rs::cosh_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::cr_math::CrMath"
reason = "CORE-MATH wrappers used by the float kernels. Exercised transitively by every conformance test that calls `sin`/`cos`/`exp`/`log`."
covered_by = "tests/conformance_arithmetic.rs::add_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::cross"
reason = "User-facing re-export `ferray_ufunc::cross` lacks a dedicated NumPy/scipy fixture in `fixtures/ufunc/`. Fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::cumprod"
reason = "User-facing re-export `ferray_ufunc::cumprod` lacks a dedicated NumPy/scipy fixture in `fixtures/ufunc/`. Fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::cumsum"
reason = "User-facing re-export `ferray_ufunc::cumsum` lacks a dedicated NumPy/scipy fixture in `fixtures/ufunc/`. Fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::cumulative_prod"
reason = "User-facing re-export `ferray_ufunc::cumulative_prod` lacks a dedicated NumPy/scipy fixture in `fixtures/ufunc/`. Fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::cumulative_sum"
reason = "User-facing re-export `ferray_ufunc::cumulative_sum` lacks a dedicated NumPy/scipy fixture in `fixtures/ufunc/`. Fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::deg2rad"
reason = "User-facing re-export `ferray_ufunc::deg2rad` lacks a dedicated NumPy/scipy fixture in `fixtures/ufunc/`. Fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::degrees"
reason = "User-facing re-export `ferray_ufunc::degrees` lacks a dedicated NumPy/scipy fixture in `fixtures/ufunc/`. Fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::degrees_f16"
reason = "User-facing re-export `ferray_ufunc::degrees_f16` lacks a dedicated NumPy/scipy fixture in `fixtures/ufunc/`. Fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::diff"
reason = "User-facing re-export `ferray_ufunc::diff` lacks a dedicated NumPy/scipy fixture in `fixtures/ufunc/`. Fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::dispatch::dispatch_binary_f16"
reason = "Runtime SIMD/scalar dispatch glue. Not part of the user-facing ferray-ufunc API. Exercised indirectly by every conformance test that calls a SIMD-eligible ufunc."
covered_by = "tests/conformance_arithmetic.rs::add_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::dispatch::dispatch_binary_f32"
reason = "Runtime SIMD/scalar dispatch glue. Not part of the user-facing ferray-ufunc API. Exercised indirectly by every conformance test that calls a SIMD-eligible ufunc."
covered_by = "tests/conformance_arithmetic.rs::add_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::dispatch::dispatch_binary_f64"
reason = "Runtime SIMD/scalar dispatch glue. Not part of the user-facing ferray-ufunc API. Exercised indirectly by every conformance test that calls a SIMD-eligible ufunc."
covered_by = "tests/conformance_arithmetic.rs::add_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::dispatch::dispatch_exp_fast_f32"
reason = "Runtime SIMD/scalar dispatch glue. Not part of the user-facing ferray-ufunc API. Exercised indirectly by every conformance test that calls a SIMD-eligible ufunc."
covered_by = "tests/conformance_explog.rs::exp_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::dispatch::dispatch_exp_fast_f64"
reason = "Runtime SIMD/scalar dispatch glue. Not part of the user-facing ferray-ufunc API. Exercised indirectly by every conformance test that calls a SIMD-eligible ufunc."
covered_by = "tests/conformance_explog.rs::exp_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::dispatch::dispatch_unary_f16"
reason = "Runtime SIMD/scalar dispatch glue. Not part of the user-facing ferray-ufunc API. Exercised indirectly by every conformance test that calls a SIMD-eligible ufunc."
covered_by = "tests/conformance_arithmetic.rs::add_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::dispatch::dispatch_unary_f32"
reason = "Runtime SIMD/scalar dispatch glue. Not part of the user-facing ferray-ufunc API. Exercised indirectly by every conformance test that calls a SIMD-eligible ufunc."
covered_by = "tests/conformance_arithmetic.rs::add_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::dispatch::dispatch_unary_f64"
reason = "Runtime SIMD/scalar dispatch glue. Not part of the user-facing ferray-ufunc API. Exercised indirectly by every conformance test that calls a SIMD-eligible ufunc."
covered_by = "tests/conformance_arithmetic.rs::add_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::dispatch::force_scalar"
reason = "Runtime SIMD/scalar dispatch glue. Not part of the user-facing ferray-ufunc API. Exercised indirectly by every conformance test that calls a SIMD-eligible ufunc."
covered_by = "tests/conformance_arithmetic.rs::add_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::dispatch::reset_force_scalar"
reason = "Runtime SIMD/scalar dispatch glue. Not part of the user-facing ferray-ufunc API. Exercised indirectly by every conformance test that calls a SIMD-eligible ufunc."
covered_by = "tests/conformance_arithmetic.rs::add_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::dispatch::simd_abs_f32"
reason = "Runtime SIMD/scalar dispatch glue. Not part of the user-facing ferray-ufunc API. Exercised indirectly by every conformance test that calls a SIMD-eligible ufunc."
covered_by = "tests/conformance_arithmetic.rs::add_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::dispatch::simd_abs_f64"
reason = "Runtime SIMD/scalar dispatch glue. Not part of the user-facing ferray-ufunc API. Exercised indirectly by every conformance test that calls a SIMD-eligible ufunc."
covered_by = "tests/conformance_arithmetic.rs::add_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::dispatch::simd_neg_f32"
reason = "Runtime SIMD/scalar dispatch glue. Not part of the user-facing ferray-ufunc API. Exercised indirectly by every conformance test that calls a SIMD-eligible ufunc."
covered_by = "tests/conformance_arithmetic.rs::add_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::dispatch::simd_neg_f64"
reason = "Runtime SIMD/scalar dispatch glue. Not part of the user-facing ferray-ufunc API. Exercised indirectly by every conformance test that calls a SIMD-eligible ufunc."
covered_by = "tests/conformance_arithmetic.rs::add_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::dispatch::simd_reciprocal_f32"
reason = "Runtime SIMD/scalar dispatch glue. Not part of the user-facing ferray-ufunc API. Exercised indirectly by every conformance test that calls a SIMD-eligible ufunc."
covered_by = "tests/conformance_arithmetic.rs::add_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::dispatch::simd_reciprocal_f64"
reason = "Runtime SIMD/scalar dispatch glue. Not part of the user-facing ferray-ufunc API. Exercised indirectly by every conformance test that calls a SIMD-eligible ufunc."
covered_by = "tests/conformance_arithmetic.rs::add_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::dispatch::simd_sqrt_f32"
reason = "Runtime SIMD/scalar dispatch glue. Not part of the user-facing ferray-ufunc API. Exercised indirectly by every conformance test that calls a SIMD-eligible ufunc."
covered_by = "tests/conformance_arithmetic.rs::add_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::dispatch::simd_sqrt_f64"
reason = "Runtime SIMD/scalar dispatch glue. Not part of the user-facing ferray-ufunc API. Exercised indirectly by every conformance test that calls a SIMD-eligible ufunc."
covered_by = "tests/conformance_arithmetic.rs::add_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::dispatch::simd_square_f32"
reason = "Runtime SIMD/scalar dispatch glue. Not part of the user-facing ferray-ufunc API. Exercised indirectly by every conformance test that calls a SIMD-eligible ufunc."
covered_by = "tests/conformance_arithmetic.rs::add_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::dispatch::simd_square_f64"
reason = "Runtime SIMD/scalar dispatch glue. Not part of the user-facing ferray-ufunc API. Exercised indirectly by every conformance test that calls a SIMD-eligible ufunc."
covered_by = "tests/conformance_arithmetic.rs::add_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::divide_broadcast"
reason = "User-facing re-export `ferray_ufunc::divide_broadcast` aliases a function family whose representative is exercised by the conformance test cited in covered_by. Dedicated fixture coverage for this alias is tracked under issue #751."
covered_by = "tests/conformance_arithmetic.rs::divide_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::divide_f16"
reason = "User-facing re-export `ferray_ufunc::divide_f16` aliases a function family whose representative is exercised by the conformance test cited in covered_by. Dedicated fixture coverage for this alias is tracked under issue #751."
covered_by = "tests/conformance_arithmetic.rs::divide_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::divide_into"
reason = "User-facing re-export `ferray_ufunc::divide_into` aliases a function family whose representative is exercised by the conformance test cited in covered_by. Dedicated fixture coverage for this alias is tracked under issue #751."
covered_by = "tests/conformance_arithmetic.rs::divide_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::divide_promoted"
reason = "User-facing re-export `ferray_ufunc::divide_promoted` aliases a function family whose representative is exercised by the conformance test cited in covered_by. Dedicated fixture coverage for this alias is tracked under issue #751."
covered_by = "tests/conformance_arithmetic.rs::divide_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::divide_ufunc"
reason = "User-facing re-export `ferray_ufunc::divide_ufunc` lacks a dedicated NumPy/scipy fixture in `fixtures/ufunc/`. Fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::divmod"
reason = "User-facing re-export `ferray_ufunc::divmod` lacks a dedicated NumPy/scipy fixture in `fixtures/ufunc/`. Fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::ediff1d"
reason = "User-facing re-export `ferray_ufunc::ediff1d` lacks a dedicated NumPy/scipy fixture in `fixtures/ufunc/`. Fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::equal"
reason = "User-facing re-export `ferray_ufunc::equal` lacks a dedicated NumPy/scipy fixture in `fixtures/ufunc/`. Fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::equal_broadcast"
reason = "User-facing re-export `ferray_ufunc::equal_broadcast` lacks a dedicated NumPy/scipy fixture in `fixtures/ufunc/`. Fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::errstate::ErrstateGuard"
reason = "Floating-point error state management API (NumPy `np.errstate` parity surface). Not value-equivalent to a NumPy ufunc — fixture coverage tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::errstate::FpErrorClass"
reason = "Floating-point error state management API (NumPy `np.errstate` parity surface). Not value-equivalent to a NumPy ufunc — fixture coverage tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::errstate::FpErrorState"
reason = "Floating-point error state management API (NumPy `np.errstate` parity surface). Not value-equivalent to a NumPy ufunc — fixture coverage tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::errstate::check_fp_errors"
reason = "Floating-point error state management API (NumPy `np.errstate` parity surface). Not value-equivalent to a NumPy ufunc — fixture coverage tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::errstate::geterr"
reason = "Floating-point error state management API (NumPy `np.errstate` parity surface). Not value-equivalent to a NumPy ufunc — fixture coverage tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::errstate::record_fp_event"
reason = "Floating-point error state management API (NumPy `np.errstate` parity surface). Not value-equivalent to a NumPy ufunc — fixture coverage tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::errstate::seterr"
reason = "Floating-point error state management API (NumPy `np.errstate` parity surface). Not value-equivalent to a NumPy ufunc — fixture coverage tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::errstate::take_fp_events"
reason = "Floating-point error state management API (NumPy `np.errstate` parity surface). Not value-equivalent to a NumPy ufunc — fixture coverage tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::errstate::with_errstate"
reason = "Floating-point error state management API (NumPy `np.errstate` parity surface). Not value-equivalent to a NumPy ufunc — fixture coverage tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::exp2_f16"
reason = "User-facing re-export `ferray_ufunc::exp2_f16` aliases a function family whose representative is exercised by the conformance test cited in covered_by. Dedicated fixture coverage for this alias is tracked under issue #751."
covered_by = "tests/conformance_explog.rs::exp2_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::exp_complex"
reason = "User-facing re-export `ferray_ufunc::exp_complex` lacks a dedicated NumPy/scipy fixture in `fixtures/ufunc/`. Fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::exp_f16"
reason = "User-facing re-export `ferray_ufunc::exp_f16` aliases a function family whose representative is exercised by the conformance test cited in covered_by. Dedicated fixture coverage for this alias is tracked under issue #751."
covered_by = "tests/conformance_explog.rs::exp_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::exp_fast"
reason = "User-facing re-export `ferray_ufunc::exp_fast` lacks a dedicated NumPy/scipy fixture in `fixtures/ufunc/`. Fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::exp_into"
reason = "User-facing re-export `ferray_ufunc::exp_into` aliases a function family whose representative is exercised by the conformance test cited in covered_by. Dedicated fixture coverage for this alias is tracked under issue #751."
covered_by = "tests/conformance_explog.rs::exp_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::expm1_complex"
reason = "User-facing re-export `ferray_ufunc::expm1_complex` lacks a dedicated NumPy/scipy fixture in `fixtures/ufunc/`. Fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::expm1_f16"
reason = "User-facing re-export `ferray_ufunc::expm1_f16` aliases a function family whose representative is exercised by the conformance test cited in covered_by. Dedicated fixture coverage for this alias is tracked under issue #751."
covered_by = "tests/conformance_explog.rs::expm1_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::fabs"
reason = "User-facing re-export `ferray_ufunc::fabs` lacks a dedicated NumPy/scipy fixture in `fixtures/ufunc/`. Fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::fast_exp::exp_fast_batch_f32"
reason = "Internal fast-path implementation of `exp`. Exercised through the user-facing `ferray_ufunc::exp` ufunc."
covered_by = "tests/conformance_explog.rs::exp_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::fast_exp::exp_fast_batch_f64"
reason = "Internal fast-path implementation of `exp`. Exercised through the user-facing `ferray_ufunc::exp` ufunc."
covered_by = "tests/conformance_explog.rs::exp_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::fast_exp::exp_fast_f32"
reason = "Internal fast-path implementation of `exp`. Exercised through the user-facing `ferray_ufunc::exp` ufunc."
covered_by = "tests/conformance_explog.rs::exp_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::fast_exp::exp_fast_f64"
reason = "Internal fast-path implementation of `exp`. Exercised through the user-facing `ferray_ufunc::exp` ufunc."
covered_by = "tests/conformance_explog.rs::exp_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::fast_trig::cos_fast_batch_f32"
reason = "Internal fast-path implementation of `sin`/`cos`. Exercised through the user-facing `ferray_ufunc::sin` and `ferray_ufunc::cos` ufuncs."
covered_by = "tests/conformance_trig.rs::sin_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::fast_trig::cos_fast_batch_f64"
reason = "Internal fast-path implementation of `sin`/`cos`. Exercised through the user-facing `ferray_ufunc::sin` and `ferray_ufunc::cos` ufuncs."
covered_by = "tests/conformance_trig.rs::sin_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::fast_trig::cos_fast_f32"
reason = "Internal fast-path implementation of `sin`/`cos`. Exercised through the user-facing `ferray_ufunc::sin` and `ferray_ufunc::cos` ufuncs."
covered_by = "tests/conformance_trig.rs::sin_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::fast_trig::cos_fast_f64"
reason = "Internal fast-path implementation of `sin`/`cos`. Exercised through the user-facing `ferray_ufunc::sin` and `ferray_ufunc::cos` ufuncs."
covered_by = "tests/conformance_trig.rs::sin_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::fast_trig::sin_fast_batch_f32"
reason = "Internal fast-path implementation of `sin`/`cos`. Exercised through the user-facing `ferray_ufunc::sin` and `ferray_ufunc::cos` ufuncs."
covered_by = "tests/conformance_trig.rs::sin_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::fast_trig::sin_fast_batch_f64"
reason = "Internal fast-path implementation of `sin`/`cos`. Exercised through the user-facing `ferray_ufunc::sin` and `ferray_ufunc::cos` ufuncs."
covered_by = "tests/conformance_trig.rs::sin_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::fast_trig::sin_fast_f32"
reason = "Internal fast-path implementation of `sin`/`cos`. Exercised through the user-facing `ferray_ufunc::sin` and `ferray_ufunc::cos` ufuncs."
covered_by = "tests/conformance_trig.rs::sin_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::fast_trig::sin_fast_f64"
reason = "Internal fast-path implementation of `sin`/`cos`. Exercised through the user-facing `ferray_ufunc::sin` and `ferray_ufunc::cos` ufuncs."
covered_by = "tests/conformance_trig.rs::sin_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::fftconvolve"
reason = "User-facing re-export `ferray_ufunc::fftconvolve` lacks a dedicated NumPy/scipy fixture in `fixtures/ufunc/`. Fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::float_power"
reason = "User-facing re-export `ferray_ufunc::float_power` lacks a dedicated NumPy/scipy fixture in `fixtures/ufunc/`. Fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::floor_divide"
reason = "User-facing re-export `ferray_ufunc::floor_divide` lacks a dedicated NumPy/scipy fixture in `fixtures/ufunc/`. Fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::floor_divide_f16"
reason = "User-facing re-export `ferray_ufunc::floor_divide_f16` lacks a dedicated NumPy/scipy fixture in `fixtures/ufunc/`. Fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::floor_f16"
reason = "User-facing re-export `ferray_ufunc::floor_f16` aliases a function family whose representative is exercised by the conformance test cited in covered_by. Dedicated fixture coverage for this alias is tracked under issue #751."
covered_by = "tests/conformance_floatintrinsic.rs::floor_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::fmod"
reason = "User-facing re-export `ferray_ufunc::fmod` lacks a dedicated NumPy/scipy fixture in `fixtures/ufunc/`. Fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::frexp"
reason = "User-facing re-export `ferray_ufunc::frexp` lacks a dedicated NumPy/scipy fixture in `fixtures/ufunc/`. Fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::gcd"
reason = "User-facing re-export `ferray_ufunc::gcd` lacks a dedicated NumPy/scipy fixture in `fixtures/ufunc/`. Fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::gcd_int"
reason = "User-facing re-export `ferray_ufunc::gcd_int` lacks a dedicated NumPy/scipy fixture in `fixtures/ufunc/`. Fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::gradient"
reason = "User-facing re-export `ferray_ufunc::gradient` lacks a dedicated NumPy/scipy fixture in `fixtures/ufunc/`. Fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::greater"
reason = "User-facing re-export `ferray_ufunc::greater` lacks a dedicated NumPy/scipy fixture in `fixtures/ufunc/`. Fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::greater_broadcast"
reason = "User-facing re-export `ferray_ufunc::greater_broadcast` lacks a dedicated NumPy/scipy fixture in `fixtures/ufunc/`. Fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::greater_equal"
reason = "User-facing re-export `ferray_ufunc::greater_equal` lacks a dedicated NumPy/scipy fixture in `fixtures/ufunc/`. Fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::greater_equal_broadcast"
reason = "User-facing re-export `ferray_ufunc::greater_equal_broadcast` lacks a dedicated NumPy/scipy fixture in `fixtures/ufunc/`. Fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::helpers::binary_broadcast_map_op"
reason = "Internal map/iter helper used to build the user-facing ufuncs. Not part of the public API contract."
covered_by = "tests/conformance_arithmetic.rs::add_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::helpers::binary_broadcast_op"
reason = "Internal map/iter helper used to build the user-facing ufuncs. Not part of the public API contract."
covered_by = "tests/conformance_arithmetic.rs::add_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::helpers::binary_elementwise_op"
reason = "Internal map/iter helper used to build the user-facing ufuncs. Not part of the public API contract."
covered_by = "tests/conformance_arithmetic.rs::add_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::helpers::binary_elementwise_op_into"
reason = "Internal map/iter helper used to build the user-facing ufuncs. Not part of the public API contract."
covered_by = "tests/conformance_arithmetic.rs::add_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::helpers::binary_f16_op"
reason = "Internal map/iter helper used to build the user-facing ufuncs. Not part of the public API contract."
covered_by = "tests/conformance_arithmetic.rs::add_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::helpers::binary_f16_to_bool_op"
reason = "Internal map/iter helper used to build the user-facing ufuncs. Not part of the public API contract."
covered_by = "tests/conformance_arithmetic.rs::add_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::helpers::binary_map_op"
reason = "Internal map/iter helper used to build the user-facing ufuncs. Not part of the public API contract."
covered_by = "tests/conformance_arithmetic.rs::add_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::helpers::binary_mixed_op"
reason = "Internal map/iter helper used to build the user-facing ufuncs. Not part of the public API contract."
covered_by = "tests/conformance_arithmetic.rs::add_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::helpers::try_simd_f32_binary"
reason = "Internal map/iter helper used to build the user-facing ufuncs. Not part of the public API contract."
covered_by = "tests/conformance_arithmetic.rs::add_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::helpers::try_simd_f32_unary"
reason = "Internal map/iter helper used to build the user-facing ufuncs. Not part of the public API contract."
covered_by = "tests/conformance_arithmetic.rs::add_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::helpers::try_simd_f64_binary"
reason = "Internal map/iter helper used to build the user-facing ufuncs. Not part of the public API contract."
covered_by = "tests/conformance_arithmetic.rs::add_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::helpers::try_simd_f64_unary"
reason = "Internal map/iter helper used to build the user-facing ufuncs. Not part of the public API contract."
covered_by = "tests/conformance_arithmetic.rs::add_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::helpers::unary_f16_op"
reason = "Internal map/iter helper used to build the user-facing ufuncs. Not part of the public API contract."
covered_by = "tests/conformance_arithmetic.rs::add_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::helpers::unary_f16_to_bool_op"
reason = "Internal map/iter helper used to build the user-facing ufuncs. Not part of the public API contract."
covered_by = "tests/conformance_arithmetic.rs::add_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::helpers::unary_float_op"
reason = "Internal map/iter helper used to build the user-facing ufuncs. Not part of the public API contract."
covered_by = "tests/conformance_arithmetic.rs::add_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::helpers::unary_float_op_compute"
reason = "Internal map/iter helper used to build the user-facing ufuncs. Not part of the public API contract."
covered_by = "tests/conformance_arithmetic.rs::add_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::helpers::unary_float_op_into"
reason = "Internal map/iter helper used to build the user-facing ufuncs. Not part of the public API contract."
covered_by = "tests/conformance_arithmetic.rs::add_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::helpers::unary_float_op_into_compute"
reason = "Internal map/iter helper used to build the user-facing ufuncs. Not part of the public API contract."
covered_by = "tests/conformance_arithmetic.rs::add_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::helpers::unary_map_op"
reason = "Internal map/iter helper used to build the user-facing ufuncs. Not part of the public API contract."
covered_by = "tests/conformance_arithmetic.rs::add_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::helpers::unary_slice_op_f32"
reason = "Internal map/iter helper used to build the user-facing ufuncs. Not part of the public API contract."
covered_by = "tests/conformance_arithmetic.rs::add_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::helpers::unary_slice_op_f64"
reason = "Internal map/iter helper used to build the user-facing ufuncs. Not part of the public API contract."
covered_by = "tests/conformance_arithmetic.rs::add_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::hypot"
reason = "User-facing re-export `ferray_ufunc::hypot` lacks a dedicated NumPy/scipy fixture in `fixtures/ufunc/`. Fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::hypot_f16"
reason = "User-facing re-export `ferray_ufunc::hypot_f16` lacks a dedicated NumPy/scipy fixture in `fixtures/ufunc/`. Fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::i0"
reason = "User-facing re-export `ferray_ufunc::i0` lacks a dedicated NumPy/scipy fixture in `fixtures/ufunc/`. Fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::imag"
reason = "User-facing re-export `ferray_ufunc::imag` lacks a dedicated NumPy/scipy fixture in `fixtures/ufunc/`. Fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::interp"
reason = "User-facing re-export `ferray_ufunc::interp` lacks a dedicated NumPy/scipy fixture in `fixtures/ufunc/`. Fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::interp_one"
reason = "User-facing re-export `ferray_ufunc::interp_one` lacks a dedicated NumPy/scipy fixture in `fixtures/ufunc/`. Fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::invert"
reason = "User-facing re-export `ferray_ufunc::invert` lacks a dedicated NumPy/scipy fixture in `fixtures/ufunc/`. Fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::isclose"
reason = "User-facing re-export `ferray_ufunc::isclose` lacks a dedicated NumPy/scipy fixture in `fixtures/ufunc/`. Fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::isclose_broadcast"
reason = "User-facing re-export `ferray_ufunc::isclose_broadcast` lacks a dedicated NumPy/scipy fixture in `fixtures/ufunc/`. Fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::iscomplex"
reason = "User-facing re-export `ferray_ufunc::iscomplex` lacks a dedicated NumPy/scipy fixture in `fixtures/ufunc/`. Fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::iscomplex_real"
reason = "User-facing re-export `ferray_ufunc::iscomplex_real` lacks a dedicated NumPy/scipy fixture in `fixtures/ufunc/`. Fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::iscomplexobj"
reason = "User-facing re-export `ferray_ufunc::iscomplexobj` lacks a dedicated NumPy/scipy fixture in `fixtures/ufunc/`. Fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::isfinite_f16"
reason = "User-facing re-export `ferray_ufunc::isfinite_f16` aliases a function family whose representative is exercised by the conformance test cited in covered_by. Dedicated fixture coverage for this alias is tracked under issue #751."
covered_by = "tests/conformance_predicates.rs::isfinite_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::isinf_f16"
reason = "User-facing re-export `ferray_ufunc::isinf_f16` aliases a function family whose representative is exercised by the conformance test cited in covered_by. Dedicated fixture coverage for this alias is tracked under issue #751."
covered_by = "tests/conformance_predicates.rs::isinf_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::isnan_f16"
reason = "User-facing re-export `ferray_ufunc::isnan_f16` aliases a function family whose representative is exercised by the conformance test cited in covered_by. Dedicated fixture coverage for this alias is tracked under issue #751."
covered_by = "tests/conformance_predicates.rs::isnan_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::isnat_datetime"
reason = "User-facing re-export `ferray_ufunc::isnat_datetime` lacks a dedicated NumPy/scipy fixture in `fixtures/ufunc/`. Fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::isnat_timedelta"
reason = "User-facing re-export `ferray_ufunc::isnat_timedelta` lacks a dedicated NumPy/scipy fixture in `fixtures/ufunc/`. Fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::isneginf"
reason = "User-facing re-export `ferray_ufunc::isneginf` lacks a dedicated NumPy/scipy fixture in `fixtures/ufunc/`. Fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::isposinf"
reason = "User-facing re-export `ferray_ufunc::isposinf` lacks a dedicated NumPy/scipy fixture in `fixtures/ufunc/`. Fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::isreal"
reason = "User-facing re-export `ferray_ufunc::isreal` lacks a dedicated NumPy/scipy fixture in `fixtures/ufunc/`. Fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::isreal_real"
reason = "User-facing re-export `ferray_ufunc::isreal_real` lacks a dedicated NumPy/scipy fixture in `fixtures/ufunc/`. Fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::isrealobj"
reason = "User-facing re-export `ferray_ufunc::isrealobj` lacks a dedicated NumPy/scipy fixture in `fixtures/ufunc/`. Fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::isscalar"
reason = "User-facing re-export `ferray_ufunc::isscalar` lacks a dedicated NumPy/scipy fixture in `fixtures/ufunc/`. Fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::kernels::scalar::binary_map"
reason = "Internal kernel module. Each kernel is dispatched through `ferray_ufunc::dispatch` and exercised by the conformance test for the user-facing ufunc it backs."
covered_by = "tests/conformance_arithmetic.rs::add_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::kernels::scalar::unary_map"
reason = "Internal kernel module. Each kernel is dispatched through `ferray_ufunc::dispatch` and exercised by the conformance test for the user-facing ufunc it backs."
covered_by = "tests/conformance_arithmetic.rs::add_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::kernels::simd_f32::add_f32"
reason = "Internal kernel module. Each kernel is dispatched through `ferray_ufunc::dispatch` and exercised by the conformance test for the user-facing ufunc it backs."
covered_by = "tests/conformance_arithmetic.rs::add_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::kernels::simd_f32::cos_f32"
reason = "Internal kernel module. Each kernel is dispatched through `ferray_ufunc::dispatch` and exercised by the conformance test for the user-facing ufunc it backs."
covered_by = "tests/conformance_trig.rs::sin_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::kernels::simd_f32::div_f32"
reason = "Internal kernel module. Each kernel is dispatched through `ferray_ufunc::dispatch` and exercised by the conformance test for the user-facing ufunc it backs."
covered_by = "tests/conformance_arithmetic.rs::add_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::kernels::simd_f32::exp_f32"
reason = "Internal kernel module. Each kernel is dispatched through `ferray_ufunc::dispatch` and exercised by the conformance test for the user-facing ufunc it backs."
covered_by = "tests/conformance_explog.rs::exp_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::kernels::simd_f32::log_f32"
reason = "Internal kernel module. Each kernel is dispatched through `ferray_ufunc::dispatch` and exercised by the conformance test for the user-facing ufunc it backs."
covered_by = "tests/conformance_explog.rs::exp_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::kernels::simd_f32::mul_f32"
reason = "Internal kernel module. Each kernel is dispatched through `ferray_ufunc::dispatch` and exercised by the conformance test for the user-facing ufunc it backs."
covered_by = "tests/conformance_arithmetic.rs::add_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::kernels::simd_f32::sin_f32"
reason = "Internal kernel module. Each kernel is dispatched through `ferray_ufunc::dispatch` and exercised by the conformance test for the user-facing ufunc it backs."
covered_by = "tests/conformance_trig.rs::sin_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::kernels::simd_f32::sqrt_f32"
reason = "Internal kernel module. Each kernel is dispatched through `ferray_ufunc::dispatch` and exercised by the conformance test for the user-facing ufunc it backs."
covered_by = "tests/conformance_arithmetic.rs::add_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::kernels::simd_f32::sub_f32"
reason = "Internal kernel module. Each kernel is dispatched through `ferray_ufunc::dispatch` and exercised by the conformance test for the user-facing ufunc it backs."
covered_by = "tests/conformance_arithmetic.rs::add_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::kernels::simd_f64::add_f64"
reason = "Internal kernel module. Each kernel is dispatched through `ferray_ufunc::dispatch` and exercised by the conformance test for the user-facing ufunc it backs."
covered_by = "tests/conformance_arithmetic.rs::add_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::kernels::simd_f64::cos_f64"
reason = "Internal kernel module. Each kernel is dispatched through `ferray_ufunc::dispatch` and exercised by the conformance test for the user-facing ufunc it backs."
covered_by = "tests/conformance_trig.rs::sin_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::kernels::simd_f64::div_f64"
reason = "Internal kernel module. Each kernel is dispatched through `ferray_ufunc::dispatch` and exercised by the conformance test for the user-facing ufunc it backs."
covered_by = "tests/conformance_arithmetic.rs::add_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::kernels::simd_f64::exp_f64"
reason = "Internal kernel module. Each kernel is dispatched through `ferray_ufunc::dispatch` and exercised by the conformance test for the user-facing ufunc it backs."
covered_by = "tests/conformance_explog.rs::exp_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::kernels::simd_f64::log_f64"
reason = "Internal kernel module. Each kernel is dispatched through `ferray_ufunc::dispatch` and exercised by the conformance test for the user-facing ufunc it backs."
covered_by = "tests/conformance_explog.rs::exp_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::kernels::simd_f64::mul_f64"
reason = "Internal kernel module. Each kernel is dispatched through `ferray_ufunc::dispatch` and exercised by the conformance test for the user-facing ufunc it backs."
covered_by = "tests/conformance_arithmetic.rs::add_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::kernels::simd_f64::sin_f64"
reason = "Internal kernel module. Each kernel is dispatched through `ferray_ufunc::dispatch` and exercised by the conformance test for the user-facing ufunc it backs."
covered_by = "tests/conformance_trig.rs::sin_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::kernels::simd_f64::sqrt_f64"
reason = "Internal kernel module. Each kernel is dispatched through `ferray_ufunc::dispatch` and exercised by the conformance test for the user-facing ufunc it backs."
covered_by = "tests/conformance_arithmetic.rs::add_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::kernels::simd_f64::sub_f64"
reason = "Internal kernel module. Each kernel is dispatched through `ferray_ufunc::dispatch` and exercised by the conformance test for the user-facing ufunc it backs."
covered_by = "tests/conformance_arithmetic.rs::add_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::kernels::simd_i32::add_i32"
reason = "Internal kernel module. Each kernel is dispatched through `ferray_ufunc::dispatch` and exercised by the conformance test for the user-facing ufunc it backs."
covered_by = "tests/conformance_arithmetic.rs::add_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::kernels::simd_i32::bitand_i32"
reason = "Internal kernel module. Each kernel is dispatched through `ferray_ufunc::dispatch` and exercised by the conformance test for the user-facing ufunc it backs."
covered_by = "tests/conformance_trig.rs::sin_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::kernels::simd_i32::bitor_i32"
reason = "Internal kernel module. Each kernel is dispatched through `ferray_ufunc::dispatch` and exercised by the conformance test for the user-facing ufunc it backs."
covered_by = "tests/conformance_arithmetic.rs::add_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::kernels::simd_i32::bitxor_i32"
reason = "Internal kernel module. Each kernel is dispatched through `ferray_ufunc::dispatch` and exercised by the conformance test for the user-facing ufunc it backs."
covered_by = "tests/conformance_arithmetic.rs::add_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::kernels::simd_i32::mul_i32"
reason = "Internal kernel module. Each kernel is dispatched through `ferray_ufunc::dispatch` and exercised by the conformance test for the user-facing ufunc it backs."
covered_by = "tests/conformance_arithmetic.rs::add_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::kernels::simd_i32::sub_i32"
reason = "Internal kernel module. Each kernel is dispatched through `ferray_ufunc::dispatch` and exercised by the conformance test for the user-facing ufunc it backs."
covered_by = "tests/conformance_arithmetic.rs::add_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::kernels::simd_i64::add_i64"
reason = "Internal kernel module. Each kernel is dispatched through `ferray_ufunc::dispatch` and exercised by the conformance test for the user-facing ufunc it backs."
covered_by = "tests/conformance_arithmetic.rs::add_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::kernels::simd_i64::bitand_i64"
reason = "Internal kernel module. Each kernel is dispatched through `ferray_ufunc::dispatch` and exercised by the conformance test for the user-facing ufunc it backs."
covered_by = "tests/conformance_trig.rs::sin_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::kernels::simd_i64::bitor_i64"
reason = "Internal kernel module. Each kernel is dispatched through `ferray_ufunc::dispatch` and exercised by the conformance test for the user-facing ufunc it backs."
covered_by = "tests/conformance_arithmetic.rs::add_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::kernels::simd_i64::bitxor_i64"
reason = "Internal kernel module. Each kernel is dispatched through `ferray_ufunc::dispatch` and exercised by the conformance test for the user-facing ufunc it backs."
covered_by = "tests/conformance_arithmetic.rs::add_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::kernels::simd_i64::mul_i64"
reason = "Internal kernel module. Each kernel is dispatched through `ferray_ufunc::dispatch` and exercised by the conformance test for the user-facing ufunc it backs."
covered_by = "tests/conformance_arithmetic.rs::add_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::kernels::simd_i64::sub_i64"
reason = "Internal kernel module. Each kernel is dispatched through `ferray_ufunc::dispatch` and exercised by the conformance test for the user-facing ufunc it backs."
covered_by = "tests/conformance_arithmetic.rs::add_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::lcm"
reason = "User-facing re-export `ferray_ufunc::lcm` lacks a dedicated NumPy/scipy fixture in `fixtures/ufunc/`. Fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::lcm_int"
reason = "User-facing re-export `ferray_ufunc::lcm_int` lacks a dedicated NumPy/scipy fixture in `fixtures/ufunc/`. Fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::ldexp"
reason = "User-facing re-export `ferray_ufunc::ldexp` lacks a dedicated NumPy/scipy fixture in `fixtures/ufunc/`. Fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::left_shift"
reason = "User-facing re-export `ferray_ufunc::left_shift` lacks a dedicated NumPy/scipy fixture in `fixtures/ufunc/`. Fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::less"
reason = "User-facing re-export `ferray_ufunc::less` lacks a dedicated NumPy/scipy fixture in `fixtures/ufunc/`. Fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::less_broadcast"
reason = "User-facing re-export `ferray_ufunc::less_broadcast` lacks a dedicated NumPy/scipy fixture in `fixtures/ufunc/`. Fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::less_equal"
reason = "User-facing re-export `ferray_ufunc::less_equal` lacks a dedicated NumPy/scipy fixture in `fixtures/ufunc/`. Fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::less_equal_broadcast"
reason = "User-facing re-export `ferray_ufunc::less_equal_broadcast` lacks a dedicated NumPy/scipy fixture in `fixtures/ufunc/`. Fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::ln_complex"
reason = "User-facing re-export `ferray_ufunc::ln_complex` lacks a dedicated NumPy/scipy fixture in `fixtures/ufunc/`. Fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::log10_complex"
reason = "User-facing re-export `ferray_ufunc::log10_complex` lacks a dedicated NumPy/scipy fixture in `fixtures/ufunc/`. Fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::log10_f16"
reason = "User-facing re-export `ferray_ufunc::log10_f16` aliases a function family whose representative is exercised by the conformance test cited in covered_by. Dedicated fixture coverage for this alias is tracked under issue #751."
covered_by = "tests/conformance_explog.rs::log10_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::log1p_complex"
reason = "User-facing re-export `ferray_ufunc::log1p_complex` lacks a dedicated NumPy/scipy fixture in `fixtures/ufunc/`. Fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::log1p_f16"
reason = "User-facing re-export `ferray_ufunc::log1p_f16` aliases a function family whose representative is exercised by the conformance test cited in covered_by. Dedicated fixture coverage for this alias is tracked under issue #751."
covered_by = "tests/conformance_explog.rs::log1p_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::log2_complex"
reason = "User-facing re-export `ferray_ufunc::log2_complex` lacks a dedicated NumPy/scipy fixture in `fixtures/ufunc/`. Fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::log2_f16"
reason = "User-facing re-export `ferray_ufunc::log2_f16` aliases a function family whose representative is exercised by the conformance test cited in covered_by. Dedicated fixture coverage for this alias is tracked under issue #751."
covered_by = "tests/conformance_explog.rs::log2_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::log_f16"
reason = "User-facing re-export `ferray_ufunc::log_f16` aliases a function family whose representative is exercised by the conformance test cited in covered_by. Dedicated fixture coverage for this alias is tracked under issue #751."
covered_by = "tests/conformance_explog.rs::log_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::log_into"
reason = "User-facing re-export `ferray_ufunc::log_into` aliases a function family whose representative is exercised by the conformance test cited in covered_by. Dedicated fixture coverage for this alias is tracked under issue #751."
covered_by = "tests/conformance_explog.rs::log_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::logaddexp"
reason = "User-facing re-export `ferray_ufunc::logaddexp` lacks a dedicated NumPy/scipy fixture in `fixtures/ufunc/`. Fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::logaddexp2"
reason = "User-facing re-export `ferray_ufunc::logaddexp2` lacks a dedicated NumPy/scipy fixture in `fixtures/ufunc/`. Fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::logical_and"
reason = "User-facing re-export `ferray_ufunc::logical_and` lacks a dedicated NumPy/scipy fixture in `fixtures/ufunc/`. Fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::logical_not"
reason = "User-facing re-export `ferray_ufunc::logical_not` lacks a dedicated NumPy/scipy fixture in `fixtures/ufunc/`. Fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::logical_or"
reason = "User-facing re-export `ferray_ufunc::logical_or` lacks a dedicated NumPy/scipy fixture in `fixtures/ufunc/`. Fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::logical_xor"
reason = "User-facing re-export `ferray_ufunc::logical_xor` lacks a dedicated NumPy/scipy fixture in `fixtures/ufunc/`. Fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::maximum_f16"
reason = "User-facing re-export `ferray_ufunc::maximum_f16` aliases a function family whose representative is exercised by the conformance test cited in covered_by. Dedicated fixture coverage for this alias is tracked under issue #751."
covered_by = "tests/conformance_comparison.rs::maximum_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::minimum_f16"
reason = "User-facing re-export `ferray_ufunc::minimum_f16` aliases a function family whose representative is exercised by the conformance test cited in covered_by. Dedicated fixture coverage for this alias is tracked under issue #751."
covered_by = "tests/conformance_comparison.rs::minimum_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::modf"
reason = "User-facing re-export `ferray_ufunc::modf` lacks a dedicated NumPy/scipy fixture in `fixtures/ufunc/`. Fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::multiply_broadcast"
reason = "User-facing re-export `ferray_ufunc::multiply_broadcast` aliases a function family whose representative is exercised by the conformance test cited in covered_by. Dedicated fixture coverage for this alias is tracked under issue #751."
covered_by = "tests/conformance_arithmetic.rs::multiply_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::multiply_f16"
reason = "User-facing re-export `ferray_ufunc::multiply_f16` aliases a function family whose representative is exercised by the conformance test cited in covered_by. Dedicated fixture coverage for this alias is tracked under issue #751."
covered_by = "tests/conformance_arithmetic.rs::multiply_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::multiply_into"
reason = "User-facing re-export `ferray_ufunc::multiply_into` aliases a function family whose representative is exercised by the conformance test cited in covered_by. Dedicated fixture coverage for this alias is tracked under issue #751."
covered_by = "tests/conformance_arithmetic.rs::multiply_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::multiply_outer"
reason = "User-facing re-export `ferray_ufunc::multiply_outer` aliases a function family whose representative is exercised by the conformance test cited in covered_by. Dedicated fixture coverage for this alias is tracked under issue #751."
covered_by = "tests/conformance_arithmetic.rs::multiply_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::multiply_promoted"
reason = "User-facing re-export `ferray_ufunc::multiply_promoted` aliases a function family whose representative is exercised by the conformance test cited in covered_by. Dedicated fixture coverage for this alias is tracked under issue #751."
covered_by = "tests/conformance_arithmetic.rs::multiply_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::multiply_ufunc"
reason = "User-facing re-export `ferray_ufunc::multiply_ufunc` lacks a dedicated NumPy/scipy fixture in `fixtures/ufunc/`. Fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::nan_add_reduce"
reason = "User-facing re-export `ferray_ufunc::nan_add_reduce` aliases a function family whose representative is exercised by the conformance test cited in covered_by. Dedicated fixture coverage for this alias is tracked under issue #751."
covered_by = "tests/conformance_arithmetic.rs::add_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::nan_add_reduce_all"
reason = "User-facing re-export `ferray_ufunc::nan_add_reduce_all` aliases a function family whose representative is exercised by the conformance test cited in covered_by. Dedicated fixture coverage for this alias is tracked under issue #751."
covered_by = "tests/conformance_arithmetic.rs::add_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::nan_add_reduce_axes"
reason = "User-facing re-export `ferray_ufunc::nan_add_reduce_axes` aliases a function family whose representative is exercised by the conformance test cited in covered_by. Dedicated fixture coverage for this alias is tracked under issue #751."
covered_by = "tests/conformance_arithmetic.rs::add_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::nan_max_reduce"
reason = "User-facing re-export `ferray_ufunc::nan_max_reduce` lacks a dedicated NumPy/scipy fixture in `fixtures/ufunc/`. Fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::nan_max_reduce_all"
reason = "User-facing re-export `ferray_ufunc::nan_max_reduce_all` lacks a dedicated NumPy/scipy fixture in `fixtures/ufunc/`. Fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::nan_max_reduce_axes"
reason = "User-facing re-export `ferray_ufunc::nan_max_reduce_axes` lacks a dedicated NumPy/scipy fixture in `fixtures/ufunc/`. Fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::nan_min_reduce"
reason = "User-facing re-export `ferray_ufunc::nan_min_reduce` lacks a dedicated NumPy/scipy fixture in `fixtures/ufunc/`. Fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::nan_min_reduce_all"
reason = "User-facing re-export `ferray_ufunc::nan_min_reduce_all` lacks a dedicated NumPy/scipy fixture in `fixtures/ufunc/`. Fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::nan_min_reduce_axes"
reason = "User-facing re-export `ferray_ufunc::nan_min_reduce_axes` lacks a dedicated NumPy/scipy fixture in `fixtures/ufunc/`. Fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::nan_multiply_reduce"
reason = "User-facing re-export `ferray_ufunc::nan_multiply_reduce` aliases a function family whose representative is exercised by the conformance test cited in covered_by. Dedicated fixture coverage for this alias is tracked under issue #751."
covered_by = "tests/conformance_arithmetic.rs::multiply_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::nan_multiply_reduce_all"
reason = "User-facing re-export `ferray_ufunc::nan_multiply_reduce_all` aliases a function family whose representative is exercised by the conformance test cited in covered_by. Dedicated fixture coverage for this alias is tracked under issue #751."
covered_by = "tests/conformance_arithmetic.rs::multiply_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::nan_multiply_reduce_axes"
reason = "User-facing re-export `ferray_ufunc::nan_multiply_reduce_axes` aliases a function family whose representative is exercised by the conformance test cited in covered_by. Dedicated fixture coverage for this alias is tracked under issue #751."
covered_by = "tests/conformance_arithmetic.rs::multiply_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::nan_to_num_f16"
reason = "User-facing re-export `ferray_ufunc::nan_to_num_f16` aliases a function family whose representative is exercised by the conformance test cited in covered_by. Dedicated fixture coverage for this alias is tracked under issue #751."
covered_by = "tests/conformance_floatintrinsic.rs::nan_to_num_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::nancumprod"
reason = "User-facing re-export `ferray_ufunc::nancumprod` lacks a dedicated NumPy/scipy fixture in `fixtures/ufunc/`. Fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::nancumsum"
reason = "User-facing re-export `ferray_ufunc::nancumsum` lacks a dedicated NumPy/scipy fixture in `fixtures/ufunc/`. Fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::negative_f16"
reason = "User-facing re-export `ferray_ufunc::negative_f16` aliases a function family whose representative is exercised by the conformance test cited in covered_by. Dedicated fixture coverage for this alias is tracked under issue #751."
covered_by = "tests/conformance_arithmetic.rs::negative_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::negative_into"
reason = "User-facing re-export `ferray_ufunc::negative_into` aliases a function family whose representative is exercised by the conformance test cited in covered_by. Dedicated fixture coverage for this alias is tracked under issue #751."
covered_by = "tests/conformance_arithmetic.rs::negative_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::nextafter"
reason = "User-facing re-export `ferray_ufunc::nextafter` lacks a dedicated NumPy/scipy fixture in `fixtures/ufunc/`. Fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::not_equal"
reason = "User-facing re-export `ferray_ufunc::not_equal` lacks a dedicated NumPy/scipy fixture in `fixtures/ufunc/`. Fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::not_equal_broadcast"
reason = "User-facing re-export `ferray_ufunc::not_equal_broadcast` lacks a dedicated NumPy/scipy fixture in `fixtures/ufunc/`. Fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::operator_overloads::array_add"
reason = "Operator-style convenience wrappers (`array_add` etc.) that forward directly to the corresponding user-facing ufunc. Tested through the underlying ufunc's conformance test."
covered_by = "tests/conformance_arithmetic.rs::add_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::operator_overloads::array_bitand"
reason = "Operator-style convenience wrappers (`array_add` etc.) that forward directly to the corresponding user-facing ufunc. Tested through the underlying ufunc's conformance test."
covered_by = "tests/conformance_arithmetic.rs::add_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::operator_overloads::array_bitnot"
reason = "Operator-style convenience wrappers (`array_add` etc.) that forward directly to the corresponding user-facing ufunc. Tested through the underlying ufunc's conformance test."
covered_by = "tests/conformance_arithmetic.rs::add_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::operator_overloads::array_bitor"
reason = "Operator-style convenience wrappers (`array_add` etc.) that forward directly to the corresponding user-facing ufunc. Tested through the underlying ufunc's conformance test."
covered_by = "tests/conformance_arithmetic.rs::add_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::operator_overloads::array_bitxor"
reason = "Operator-style convenience wrappers (`array_add` etc.) that forward directly to the corresponding user-facing ufunc. Tested through the underlying ufunc's conformance test."
covered_by = "tests/conformance_arithmetic.rs::add_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::operator_overloads::array_div"
reason = "Operator-style convenience wrappers (`array_add` etc.) that forward directly to the corresponding user-facing ufunc. Tested through the underlying ufunc's conformance test."
covered_by = "tests/conformance_arithmetic.rs::add_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::operator_overloads::array_mul"
reason = "Operator-style convenience wrappers (`array_add` etc.) that forward directly to the corresponding user-facing ufunc. Tested through the underlying ufunc's conformance test."
covered_by = "tests/conformance_arithmetic.rs::add_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::operator_overloads::array_neg"
reason = "Operator-style convenience wrappers (`array_add` etc.) that forward directly to the corresponding user-facing ufunc. Tested through the underlying ufunc's conformance test."
covered_by = "tests/conformance_arithmetic.rs::add_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::operator_overloads::array_rem"
reason = "Operator-style convenience wrappers (`array_add` etc.) that forward directly to the corresponding user-facing ufunc. Tested through the underlying ufunc's conformance test."
covered_by = "tests/conformance_arithmetic.rs::add_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::operator_overloads::array_shl"
reason = "Operator-style convenience wrappers (`array_add` etc.) that forward directly to the corresponding user-facing ufunc. Tested through the underlying ufunc's conformance test."
covered_by = "tests/conformance_arithmetic.rs::add_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::operator_overloads::array_shr"
reason = "Operator-style convenience wrappers (`array_add` etc.) that forward directly to the corresponding user-facing ufunc. Tested through the underlying ufunc's conformance test."
covered_by = "tests/conformance_arithmetic.rs::add_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::operator_overloads::array_sub"
reason = "Operator-style convenience wrappers (`array_add` etc.) that forward directly to the corresponding user-facing ufunc. Tested through the underlying ufunc's conformance test."
covered_by = "tests/conformance_arithmetic.rs::add_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::ops::arithmetic::absolute_into"
reason = "Inner canonical path in `ferray_ufunc::ops::arithmetic`. The user-facing API is the re-export at `ferray_ufunc::absolute_into`, which is exercised by the conformance test cited in covered_by; the same function symbol is bound through both paths so the inner path is implicitly verified."
covered_by = "tests/conformance_arithmetic.rs::absolute_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::ops::arithmetic::add_accumulate"
reason = "Inner canonical path in `ferray_ufunc::ops::arithmetic`. The user-facing API is the re-export at `ferray_ufunc::add_accumulate`, which is exercised by the conformance test cited in covered_by; the same function symbol is bound through both paths so the inner path is implicitly verified."
covered_by = "tests/conformance_arithmetic.rs::add_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::ops::arithmetic::add_broadcast"
reason = "Inner canonical path in `ferray_ufunc::ops::arithmetic`. The user-facing API is the re-export at `ferray_ufunc::add_broadcast`, which is exercised by the conformance test cited in covered_by; the same function symbol is bound through both paths so the inner path is implicitly verified."
covered_by = "tests/conformance_arithmetic.rs::add_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::ops::arithmetic::add_into"
reason = "Inner canonical path in `ferray_ufunc::ops::arithmetic`. The user-facing API is the re-export at `ferray_ufunc::add_into`, which is exercised by the conformance test cited in covered_by; the same function symbol is bound through both paths so the inner path is implicitly verified."
covered_by = "tests/conformance_arithmetic.rs::add_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::ops::arithmetic::add_reduce"
reason = "Inner canonical path in `ferray_ufunc::ops::arithmetic`. The user-facing API is the re-export at `ferray_ufunc::add_reduce`, which is exercised by the conformance test cited in covered_by; the same function symbol is bound through both paths so the inner path is implicitly verified."
covered_by = "tests/conformance_arithmetic.rs::add_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::ops::arithmetic::add_reduce_all"
reason = "Inner canonical path in `ferray_ufunc::ops::arithmetic`. The user-facing API is the re-export at `ferray_ufunc::add_reduce_all`, which is exercised by the conformance test cited in covered_by; the same function symbol is bound through both paths so the inner path is implicitly verified."
covered_by = "tests/conformance_arithmetic.rs::add_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::ops::arithmetic::add_reduce_axes"
reason = "Inner canonical path in `ferray_ufunc::ops::arithmetic`. The user-facing API is the re-export at `ferray_ufunc::add_reduce_axes`, which is exercised by the conformance test cited in covered_by; the same function symbol is bound through both paths so the inner path is implicitly verified."
covered_by = "tests/conformance_arithmetic.rs::add_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::ops::arithmetic::add_reduce_keepdims"
reason = "Inner canonical path in `ferray_ufunc::ops::arithmetic`. The user-facing API is the re-export at `ferray_ufunc::add_reduce_keepdims`, which is exercised by the conformance test cited in covered_by; the same function symbol is bound through both paths so the inner path is implicitly verified."
covered_by = "tests/conformance_arithmetic.rs::add_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::ops::arithmetic::cross"
reason = "Inner canonical path in `ferray_ufunc::ops::arithmetic`. The user-facing API is the re-export at `ferray_ufunc::cross`, which is exercised by the conformance test cited in covered_by; the same function symbol is bound through both paths so the inner path is implicitly verified."
covered_by = "tests/conformance_arithmetic.rs::add_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::ops::arithmetic::cumprod"
reason = "Inner canonical path in `ferray_ufunc::ops::arithmetic`. The user-facing API is the re-export at `ferray_ufunc::cumprod`, which is exercised by the conformance test cited in covered_by; the same function symbol is bound through both paths so the inner path is implicitly verified."
covered_by = "tests/conformance_arithmetic.rs::add_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::ops::arithmetic::cumsum"
reason = "Inner canonical path in `ferray_ufunc::ops::arithmetic`. The user-facing API is the re-export at `ferray_ufunc::cumsum`, which is exercised by the conformance test cited in covered_by; the same function symbol is bound through both paths so the inner path is implicitly verified."
covered_by = "tests/conformance_arithmetic.rs::add_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::ops::arithmetic::cumulative_prod"
reason = "Inner canonical path in `ferray_ufunc::ops::arithmetic`. The user-facing API is the re-export at `ferray_ufunc::cumulative_prod`, which is exercised by the conformance test cited in covered_by; the same function symbol is bound through both paths so the inner path is implicitly verified."
covered_by = "tests/conformance_arithmetic.rs::add_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::ops::arithmetic::cumulative_sum"
reason = "Inner canonical path in `ferray_ufunc::ops::arithmetic`. The user-facing API is the re-export at `ferray_ufunc::cumulative_sum`, which is exercised by the conformance test cited in covered_by; the same function symbol is bound through both paths so the inner path is implicitly verified."
covered_by = "tests/conformance_arithmetic.rs::add_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::ops::arithmetic::diff"
reason = "Inner canonical path in `ferray_ufunc::ops::arithmetic`. The user-facing API is the re-export at `ferray_ufunc::diff`, which is exercised by the conformance test cited in covered_by; the same function symbol is bound through both paths so the inner path is implicitly verified."
covered_by = "tests/conformance_arithmetic.rs::add_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::ops::arithmetic::divide_broadcast"
reason = "Inner canonical path in `ferray_ufunc::ops::arithmetic`. The user-facing API is the re-export at `ferray_ufunc::divide_broadcast`, which is exercised by the conformance test cited in covered_by; the same function symbol is bound through both paths so the inner path is implicitly verified."
covered_by = "tests/conformance_arithmetic.rs::divide_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::ops::arithmetic::divide_into"
reason = "Inner canonical path in `ferray_ufunc::ops::arithmetic`. The user-facing API is the re-export at `ferray_ufunc::divide_into`, which is exercised by the conformance test cited in covered_by; the same function symbol is bound through both paths so the inner path is implicitly verified."
covered_by = "tests/conformance_arithmetic.rs::divide_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::ops::arithmetic::divmod"
reason = "Inner canonical path in `ferray_ufunc::ops::arithmetic`. The user-facing API is the re-export at `ferray_ufunc::divmod`, which is exercised by the conformance test cited in covered_by; the same function symbol is bound through both paths so the inner path is implicitly verified."
covered_by = "tests/conformance_arithmetic.rs::add_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::ops::arithmetic::ediff1d"
reason = "Inner canonical path in `ferray_ufunc::ops::arithmetic`. The user-facing API is the re-export at `ferray_ufunc::ediff1d`, which is exercised by the conformance test cited in covered_by; the same function symbol is bound through both paths so the inner path is implicitly verified."
covered_by = "tests/conformance_arithmetic.rs::add_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::ops::arithmetic::fabs"
reason = "Inner canonical path in `ferray_ufunc::ops::arithmetic`. The user-facing API is the re-export at `ferray_ufunc::fabs`, which is exercised by the conformance test cited in covered_by; the same function symbol is bound through both paths so the inner path is implicitly verified."
covered_by = "tests/conformance_arithmetic.rs::add_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::ops::arithmetic::floor_divide"
reason = "Inner canonical path in `ferray_ufunc::ops::arithmetic`. The user-facing API is the re-export at `ferray_ufunc::floor_divide`, which is exercised by the conformance test cited in covered_by; the same function symbol is bound through both paths so the inner path is implicitly verified."
covered_by = "tests/conformance_arithmetic.rs::add_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::ops::arithmetic::fmod"
reason = "Inner canonical path in `ferray_ufunc::ops::arithmetic`. The user-facing API is the re-export at `ferray_ufunc::fmod`, which is exercised by the conformance test cited in covered_by; the same function symbol is bound through both paths so the inner path is implicitly verified."
covered_by = "tests/conformance_arithmetic.rs::add_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::ops::arithmetic::gcd"
reason = "Inner canonical path in `ferray_ufunc::ops::arithmetic`. The user-facing API is the re-export at `ferray_ufunc::gcd`, which is exercised by the conformance test cited in covered_by; the same function symbol is bound through both paths so the inner path is implicitly verified."
covered_by = "tests/conformance_arithmetic.rs::add_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::ops::arithmetic::gcd_int"
reason = "Inner canonical path in `ferray_ufunc::ops::arithmetic`. The user-facing API is the re-export at `ferray_ufunc::gcd_int`, which is exercised by the conformance test cited in covered_by; the same function symbol is bound through both paths so the inner path is implicitly verified."
covered_by = "tests/conformance_arithmetic.rs::add_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::ops::arithmetic::gradient"
reason = "Inner canonical path in `ferray_ufunc::ops::arithmetic`. The user-facing API is the re-export at `ferray_ufunc::gradient`, which is exercised by the conformance test cited in covered_by; the same function symbol is bound through both paths so the inner path is implicitly verified."
covered_by = "tests/conformance_arithmetic.rs::add_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::ops::arithmetic::lcm"
reason = "Inner canonical path in `ferray_ufunc::ops::arithmetic`. The user-facing API is the re-export at `ferray_ufunc::lcm`, which is exercised by the conformance test cited in covered_by; the same function symbol is bound through both paths so the inner path is implicitly verified."
covered_by = "tests/conformance_arithmetic.rs::add_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::ops::arithmetic::lcm_int"
reason = "Inner canonical path in `ferray_ufunc::ops::arithmetic`. The user-facing API is the re-export at `ferray_ufunc::lcm_int`, which is exercised by the conformance test cited in covered_by; the same function symbol is bound through both paths so the inner path is implicitly verified."
covered_by = "tests/conformance_arithmetic.rs::add_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::ops::arithmetic::multiply_broadcast"
reason = "Inner canonical path in `ferray_ufunc::ops::arithmetic`. The user-facing API is the re-export at `ferray_ufunc::multiply_broadcast`, which is exercised by the conformance test cited in covered_by; the same function symbol is bound through both paths so the inner path is implicitly verified."
covered_by = "tests/conformance_arithmetic.rs::multiply_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::ops::arithmetic::multiply_into"
reason = "Inner canonical path in `ferray_ufunc::ops::arithmetic`. The user-facing API is the re-export at `ferray_ufunc::multiply_into`, which is exercised by the conformance test cited in covered_by; the same function symbol is bound through both paths so the inner path is implicitly verified."
covered_by = "tests/conformance_arithmetic.rs::multiply_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::ops::arithmetic::multiply_outer"
reason = "Inner canonical path in `ferray_ufunc::ops::arithmetic`. The user-facing API is the re-export at `ferray_ufunc::multiply_outer`, which is exercised by the conformance test cited in covered_by; the same function symbol is bound through both paths so the inner path is implicitly verified."
covered_by = "tests/conformance_arithmetic.rs::multiply_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::ops::arithmetic::nan_add_reduce"
reason = "Inner canonical path in `ferray_ufunc::ops::arithmetic`. The user-facing API is the re-export at `ferray_ufunc::nan_add_reduce`, which is exercised by the conformance test cited in covered_by; the same function symbol is bound through both paths so the inner path is implicitly verified."
covered_by = "tests/conformance_arithmetic.rs::add_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::ops::arithmetic::nan_add_reduce_all"
reason = "Inner canonical path in `ferray_ufunc::ops::arithmetic`. The user-facing API is the re-export at `ferray_ufunc::nan_add_reduce_all`, which is exercised by the conformance test cited in covered_by; the same function symbol is bound through both paths so the inner path is implicitly verified."
covered_by = "tests/conformance_arithmetic.rs::add_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::ops::arithmetic::nan_add_reduce_axes"
reason = "Inner canonical path in `ferray_ufunc::ops::arithmetic`. The user-facing API is the re-export at `ferray_ufunc::nan_add_reduce_axes`, which is exercised by the conformance test cited in covered_by; the same function symbol is bound through both paths so the inner path is implicitly verified."
covered_by = "tests/conformance_arithmetic.rs::add_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::ops::arithmetic::nan_max_reduce"
reason = "Inner canonical path in `ferray_ufunc::ops::arithmetic`. The user-facing API is the re-export at `ferray_ufunc::nan_max_reduce`, which is exercised by the conformance test cited in covered_by; the same function symbol is bound through both paths so the inner path is implicitly verified."
covered_by = "tests/conformance_arithmetic.rs::add_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::ops::arithmetic::nan_max_reduce_all"
reason = "Inner canonical path in `ferray_ufunc::ops::arithmetic`. The user-facing API is the re-export at `ferray_ufunc::nan_max_reduce_all`, which is exercised by the conformance test cited in covered_by; the same function symbol is bound through both paths so the inner path is implicitly verified."
covered_by = "tests/conformance_arithmetic.rs::add_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::ops::arithmetic::nan_max_reduce_axes"
reason = "Inner canonical path in `ferray_ufunc::ops::arithmetic`. The user-facing API is the re-export at `ferray_ufunc::nan_max_reduce_axes`, which is exercised by the conformance test cited in covered_by; the same function symbol is bound through both paths so the inner path is implicitly verified."
covered_by = "tests/conformance_arithmetic.rs::add_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::ops::arithmetic::nan_min_reduce"
reason = "Inner canonical path in `ferray_ufunc::ops::arithmetic`. The user-facing API is the re-export at `ferray_ufunc::nan_min_reduce`, which is exercised by the conformance test cited in covered_by; the same function symbol is bound through both paths so the inner path is implicitly verified."
covered_by = "tests/conformance_arithmetic.rs::add_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::ops::arithmetic::nan_min_reduce_all"
reason = "Inner canonical path in `ferray_ufunc::ops::arithmetic`. The user-facing API is the re-export at `ferray_ufunc::nan_min_reduce_all`, which is exercised by the conformance test cited in covered_by; the same function symbol is bound through both paths so the inner path is implicitly verified."
covered_by = "tests/conformance_arithmetic.rs::add_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::ops::arithmetic::nan_min_reduce_axes"
reason = "Inner canonical path in `ferray_ufunc::ops::arithmetic`. The user-facing API is the re-export at `ferray_ufunc::nan_min_reduce_axes`, which is exercised by the conformance test cited in covered_by; the same function symbol is bound through both paths so the inner path is implicitly verified."
covered_by = "tests/conformance_arithmetic.rs::add_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::ops::arithmetic::nan_multiply_reduce"
reason = "Inner canonical path in `ferray_ufunc::ops::arithmetic`. The user-facing API is the re-export at `ferray_ufunc::nan_multiply_reduce`, which is exercised by the conformance test cited in covered_by; the same function symbol is bound through both paths so the inner path is implicitly verified."
covered_by = "tests/conformance_arithmetic.rs::multiply_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::ops::arithmetic::nan_multiply_reduce_all"
reason = "Inner canonical path in `ferray_ufunc::ops::arithmetic`. The user-facing API is the re-export at `ferray_ufunc::nan_multiply_reduce_all`, which is exercised by the conformance test cited in covered_by; the same function symbol is bound through both paths so the inner path is implicitly verified."
covered_by = "tests/conformance_arithmetic.rs::multiply_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::ops::arithmetic::nan_multiply_reduce_axes"
reason = "Inner canonical path in `ferray_ufunc::ops::arithmetic`. The user-facing API is the re-export at `ferray_ufunc::nan_multiply_reduce_axes`, which is exercised by the conformance test cited in covered_by; the same function symbol is bound through both paths so the inner path is implicitly verified."
covered_by = "tests/conformance_arithmetic.rs::multiply_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::ops::arithmetic::nancumprod"
reason = "Inner canonical path in `ferray_ufunc::ops::arithmetic`. The user-facing API is the re-export at `ferray_ufunc::nancumprod`, which is exercised by the conformance test cited in covered_by; the same function symbol is bound through both paths so the inner path is implicitly verified."
covered_by = "tests/conformance_arithmetic.rs::add_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::ops::arithmetic::nancumsum"
reason = "Inner canonical path in `ferray_ufunc::ops::arithmetic`. The user-facing API is the re-export at `ferray_ufunc::nancumsum`, which is exercised by the conformance test cited in covered_by; the same function symbol is bound through both paths so the inner path is implicitly verified."
covered_by = "tests/conformance_arithmetic.rs::add_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::ops::arithmetic::negative_into"
reason = "Inner canonical path in `ferray_ufunc::ops::arithmetic`. The user-facing API is the re-export at `ferray_ufunc::negative_into`, which is exercised by the conformance test cited in covered_by; the same function symbol is bound through both paths so the inner path is implicitly verified."
covered_by = "tests/conformance_arithmetic.rs::negative_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::ops::arithmetic::positive"
reason = "Inner canonical path in `ferray_ufunc::ops::arithmetic`. The user-facing API is the re-export at `ferray_ufunc::positive`, which is exercised by the conformance test cited in covered_by; the same function symbol is bound through both paths so the inner path is implicitly verified."
covered_by = "tests/conformance_arithmetic.rs::add_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::ops::arithmetic::sign"
reason = "Inner canonical path in `ferray_ufunc::ops::arithmetic`. The user-facing API is the re-export at `ferray_ufunc::sign`, which is exercised by the conformance test cited in covered_by; the same function symbol is bound through both paths so the inner path is implicitly verified."
covered_by = "tests/conformance_arithmetic.rs::add_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::ops::arithmetic::sqrt_into"
reason = "Inner canonical path in `ferray_ufunc::ops::arithmetic`. The user-facing API is the re-export at `ferray_ufunc::sqrt_into`, which is exercised by the conformance test cited in covered_by; the same function symbol is bound through both paths so the inner path is implicitly verified."
covered_by = "tests/conformance_arithmetic.rs::sqrt_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::ops::arithmetic::square_into"
reason = "Inner canonical path in `ferray_ufunc::ops::arithmetic`. The user-facing API is the re-export at `ferray_ufunc::square_into`, which is exercised by the conformance test cited in covered_by; the same function symbol is bound through both paths so the inner path is implicitly verified."
covered_by = "tests/conformance_arithmetic.rs::square_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::ops::arithmetic::subtract_broadcast"
reason = "Inner canonical path in `ferray_ufunc::ops::arithmetic`. The user-facing API is the re-export at `ferray_ufunc::subtract_broadcast`, which is exercised by the conformance test cited in covered_by; the same function symbol is bound through both paths so the inner path is implicitly verified."
covered_by = "tests/conformance_arithmetic.rs::subtract_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::ops::arithmetic::subtract_into"
reason = "Inner canonical path in `ferray_ufunc::ops::arithmetic`. The user-facing API is the re-export at `ferray_ufunc::subtract_into`, which is exercised by the conformance test cited in covered_by; the same function symbol is bound through both paths so the inner path is implicitly verified."
covered_by = "tests/conformance_arithmetic.rs::subtract_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::ops::arithmetic::trapezoid"
reason = "Inner canonical path in `ferray_ufunc::ops::arithmetic`. The user-facing API is the re-export at `ferray_ufunc::trapezoid`, which is exercised by the conformance test cited in covered_by; the same function symbol is bound through both paths so the inner path is implicitly verified."
covered_by = "tests/conformance_arithmetic.rs::add_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::ops::arithmetic::true_divide"
reason = "Inner canonical path in `ferray_ufunc::ops::arithmetic`. The user-facing API is the re-export at `ferray_ufunc::true_divide`, which is exercised by the conformance test cited in covered_by; the same function symbol is bound through both paths so the inner path is implicitly verified."
covered_by = "tests/conformance_arithmetic.rs::add_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::ops::bitwise::BitwiseCount"
reason = "User-facing function in `ferray_ufunc::ops::bitwise`. No NumPy/scipy fixture exists yet in `fixtures/ufunc/`; fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::ops::bitwise::BitwiseOps"
reason = "User-facing function in `ferray_ufunc::ops::bitwise`. No NumPy/scipy fixture exists yet in `fixtures/ufunc/`; fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::ops::bitwise::ShiftOps"
reason = "User-facing function in `ferray_ufunc::ops::bitwise`. No NumPy/scipy fixture exists yet in `fixtures/ufunc/`; fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::ops::bitwise::bitwise_and"
reason = "User-facing function in `ferray_ufunc::ops::bitwise`. No NumPy/scipy fixture exists yet in `fixtures/ufunc/`; fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::ops::bitwise::bitwise_count"
reason = "User-facing function in `ferray_ufunc::ops::bitwise`. No NumPy/scipy fixture exists yet in `fixtures/ufunc/`; fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::ops::bitwise::bitwise_not"
reason = "User-facing function in `ferray_ufunc::ops::bitwise`. No NumPy/scipy fixture exists yet in `fixtures/ufunc/`; fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::ops::bitwise::bitwise_or"
reason = "User-facing function in `ferray_ufunc::ops::bitwise`. No NumPy/scipy fixture exists yet in `fixtures/ufunc/`; fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::ops::bitwise::bitwise_xor"
reason = "User-facing function in `ferray_ufunc::ops::bitwise`. No NumPy/scipy fixture exists yet in `fixtures/ufunc/`; fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::ops::bitwise::invert"
reason = "User-facing function in `ferray_ufunc::ops::bitwise`. No NumPy/scipy fixture exists yet in `fixtures/ufunc/`; fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::ops::bitwise::left_shift"
reason = "User-facing function in `ferray_ufunc::ops::bitwise`. No NumPy/scipy fixture exists yet in `fixtures/ufunc/`; fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::ops::bitwise::right_shift"
reason = "User-facing function in `ferray_ufunc::ops::bitwise`. No NumPy/scipy fixture exists yet in `fixtures/ufunc/`; fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::ops::comparison::allclose"
reason = "Inner canonical path in `ferray_ufunc::ops::comparison`. The user-facing API is the re-export at `ferray_ufunc::allclose`, which is exercised by the conformance test cited in covered_by; the same function symbol is bound through both paths so the inner path is implicitly verified."
covered_by = "tests/conformance_comparison.rs::maximum_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::ops::comparison::array_equal"
reason = "Inner canonical path in `ferray_ufunc::ops::comparison`. The user-facing API is the re-export at `ferray_ufunc::array_equal`, which is exercised by the conformance test cited in covered_by; the same function symbol is bound through both paths so the inner path is implicitly verified."
covered_by = "tests/conformance_comparison.rs::maximum_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::ops::comparison::array_equiv"
reason = "Inner canonical path in `ferray_ufunc::ops::comparison`. The user-facing API is the re-export at `ferray_ufunc::array_equiv`, which is exercised by the conformance test cited in covered_by; the same function symbol is bound through both paths so the inner path is implicitly verified."
covered_by = "tests/conformance_comparison.rs::maximum_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::ops::comparison::equal"
reason = "Inner canonical path in `ferray_ufunc::ops::comparison`. The user-facing API is the re-export at `ferray_ufunc::equal`, which is exercised by the conformance test cited in covered_by; the same function symbol is bound through both paths so the inner path is implicitly verified."
covered_by = "tests/conformance_comparison.rs::maximum_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::ops::comparison::equal_broadcast"
reason = "Inner canonical path in `ferray_ufunc::ops::comparison`. The user-facing API is the re-export at `ferray_ufunc::equal_broadcast`, which is exercised by the conformance test cited in covered_by; the same function symbol is bound through both paths so the inner path is implicitly verified."
covered_by = "tests/conformance_comparison.rs::maximum_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::ops::comparison::greater"
reason = "Inner canonical path in `ferray_ufunc::ops::comparison`. The user-facing API is the re-export at `ferray_ufunc::greater`, which is exercised by the conformance test cited in covered_by; the same function symbol is bound through both paths so the inner path is implicitly verified."
covered_by = "tests/conformance_comparison.rs::maximum_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::ops::comparison::greater_broadcast"
reason = "Inner canonical path in `ferray_ufunc::ops::comparison`. The user-facing API is the re-export at `ferray_ufunc::greater_broadcast`, which is exercised by the conformance test cited in covered_by; the same function symbol is bound through both paths so the inner path is implicitly verified."
covered_by = "tests/conformance_comparison.rs::maximum_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::ops::comparison::greater_equal"
reason = "Inner canonical path in `ferray_ufunc::ops::comparison`. The user-facing API is the re-export at `ferray_ufunc::greater_equal`, which is exercised by the conformance test cited in covered_by; the same function symbol is bound through both paths so the inner path is implicitly verified."
covered_by = "tests/conformance_comparison.rs::maximum_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::ops::comparison::greater_equal_broadcast"
reason = "Inner canonical path in `ferray_ufunc::ops::comparison`. The user-facing API is the re-export at `ferray_ufunc::greater_equal_broadcast`, which is exercised by the conformance test cited in covered_by; the same function symbol is bound through both paths so the inner path is implicitly verified."
covered_by = "tests/conformance_comparison.rs::maximum_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::ops::comparison::isclose"
reason = "Inner canonical path in `ferray_ufunc::ops::comparison`. The user-facing API is the re-export at `ferray_ufunc::isclose`, which is exercised by the conformance test cited in covered_by; the same function symbol is bound through both paths so the inner path is implicitly verified."
covered_by = "tests/conformance_comparison.rs::maximum_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::ops::comparison::isclose_broadcast"
reason = "Inner canonical path in `ferray_ufunc::ops::comparison`. The user-facing API is the re-export at `ferray_ufunc::isclose_broadcast`, which is exercised by the conformance test cited in covered_by; the same function symbol is bound through both paths so the inner path is implicitly verified."
covered_by = "tests/conformance_comparison.rs::maximum_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::ops::comparison::less"
reason = "Inner canonical path in `ferray_ufunc::ops::comparison`. The user-facing API is the re-export at `ferray_ufunc::less`, which is exercised by the conformance test cited in covered_by; the same function symbol is bound through both paths so the inner path is implicitly verified."
covered_by = "tests/conformance_comparison.rs::maximum_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::ops::comparison::less_broadcast"
reason = "Inner canonical path in `ferray_ufunc::ops::comparison`. The user-facing API is the re-export at `ferray_ufunc::less_broadcast`, which is exercised by the conformance test cited in covered_by; the same function symbol is bound through both paths so the inner path is implicitly verified."
covered_by = "tests/conformance_comparison.rs::maximum_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::ops::comparison::less_equal"
reason = "Inner canonical path in `ferray_ufunc::ops::comparison`. The user-facing API is the re-export at `ferray_ufunc::less_equal`, which is exercised by the conformance test cited in covered_by; the same function symbol is bound through both paths so the inner path is implicitly verified."
covered_by = "tests/conformance_comparison.rs::maximum_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::ops::comparison::less_equal_broadcast"
reason = "Inner canonical path in `ferray_ufunc::ops::comparison`. The user-facing API is the re-export at `ferray_ufunc::less_equal_broadcast`, which is exercised by the conformance test cited in covered_by; the same function symbol is bound through both paths so the inner path is implicitly verified."
covered_by = "tests/conformance_comparison.rs::maximum_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::ops::comparison::not_equal"
reason = "Inner canonical path in `ferray_ufunc::ops::comparison`. The user-facing API is the re-export at `ferray_ufunc::not_equal`, which is exercised by the conformance test cited in covered_by; the same function symbol is bound through both paths so the inner path is implicitly verified."
covered_by = "tests/conformance_comparison.rs::maximum_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::ops::comparison::not_equal_broadcast"
reason = "Inner canonical path in `ferray_ufunc::ops::comparison`. The user-facing API is the re-export at `ferray_ufunc::not_equal_broadcast`, which is exercised by the conformance test cited in covered_by; the same function symbol is bound through both paths so the inner path is implicitly verified."
covered_by = "tests/conformance_comparison.rs::maximum_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::ops::complex::abs"
reason = "User-facing function in `ferray_ufunc::ops::complex`. No NumPy/scipy fixture exists yet in `fixtures/ufunc/`; fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::ops::complex::angle"
reason = "User-facing function in `ferray_ufunc::ops::complex`. No NumPy/scipy fixture exists yet in `fixtures/ufunc/`; fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::ops::complex::conj"
reason = "User-facing function in `ferray_ufunc::ops::complex`. No NumPy/scipy fixture exists yet in `fixtures/ufunc/`; fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::ops::complex::conjugate"
reason = "User-facing function in `ferray_ufunc::ops::complex`. No NumPy/scipy fixture exists yet in `fixtures/ufunc/`; fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::ops::complex::expm1_complex"
reason = "User-facing function in `ferray_ufunc::ops::complex`. No NumPy/scipy fixture exists yet in `fixtures/ufunc/`; fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::ops::complex::imag"
reason = "User-facing function in `ferray_ufunc::ops::complex`. No NumPy/scipy fixture exists yet in `fixtures/ufunc/`; fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::ops::complex::iscomplex"
reason = "User-facing function in `ferray_ufunc::ops::complex`. No NumPy/scipy fixture exists yet in `fixtures/ufunc/`; fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::ops::complex::iscomplex_real"
reason = "User-facing function in `ferray_ufunc::ops::complex`. No NumPy/scipy fixture exists yet in `fixtures/ufunc/`; fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::ops::complex::iscomplexobj"
reason = "User-facing function in `ferray_ufunc::ops::complex`. No NumPy/scipy fixture exists yet in `fixtures/ufunc/`; fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::ops::complex::isreal"
reason = "User-facing function in `ferray_ufunc::ops::complex`. No NumPy/scipy fixture exists yet in `fixtures/ufunc/`; fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::ops::complex::isreal_real"
reason = "User-facing function in `ferray_ufunc::ops::complex`. No NumPy/scipy fixture exists yet in `fixtures/ufunc/`; fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::ops::complex::isrealobj"
reason = "User-facing function in `ferray_ufunc::ops::complex`. No NumPy/scipy fixture exists yet in `fixtures/ufunc/`; fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::ops::complex::isscalar"
reason = "User-facing function in `ferray_ufunc::ops::complex`. No NumPy/scipy fixture exists yet in `fixtures/ufunc/`; fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::ops::complex::log10_complex"
reason = "User-facing function in `ferray_ufunc::ops::complex`. No NumPy/scipy fixture exists yet in `fixtures/ufunc/`; fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::ops::complex::log1p_complex"
reason = "User-facing function in `ferray_ufunc::ops::complex`. No NumPy/scipy fixture exists yet in `fixtures/ufunc/`; fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::ops::complex::log2_complex"
reason = "User-facing function in `ferray_ufunc::ops::complex`. No NumPy/scipy fixture exists yet in `fixtures/ufunc/`; fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::ops::complex::power_complex"
reason = "User-facing function in `ferray_ufunc::ops::complex`. No NumPy/scipy fixture exists yet in `fixtures/ufunc/`; fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::ops::complex::real"
reason = "User-facing function in `ferray_ufunc::ops::complex`. No NumPy/scipy fixture exists yet in `fixtures/ufunc/`; fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::ops::convolution::ConvolveMode"
reason = "User-facing function in `ferray_ufunc::ops::convolution`. No NumPy/scipy fixture exists yet in `fixtures/ufunc/`; fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::ops::convolution::convolve"
reason = "User-facing function in `ferray_ufunc::ops::convolution`. No NumPy/scipy fixture exists yet in `fixtures/ufunc/`; fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::ops::convolution::fftconvolve"
reason = "User-facing function in `ferray_ufunc::ops::convolution`. No NumPy/scipy fixture exists yet in `fixtures/ufunc/`; fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::ops::datetime::add_datetime_timedelta"
reason = "User-facing function in `ferray_ufunc::ops::datetime`. No NumPy/scipy fixture exists yet in `fixtures/ufunc/`; fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::ops::datetime::add_datetime_timedelta_promoted"
reason = "User-facing function in `ferray_ufunc::ops::datetime`. No NumPy/scipy fixture exists yet in `fixtures/ufunc/`; fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::ops::datetime::add_timedelta"
reason = "User-facing function in `ferray_ufunc::ops::datetime`. No NumPy/scipy fixture exists yet in `fixtures/ufunc/`; fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::ops::datetime::add_timedelta_promoted"
reason = "User-facing function in `ferray_ufunc::ops::datetime`. No NumPy/scipy fixture exists yet in `fixtures/ufunc/`; fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::ops::datetime::isnat_datetime"
reason = "User-facing function in `ferray_ufunc::ops::datetime`. No NumPy/scipy fixture exists yet in `fixtures/ufunc/`; fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::ops::datetime::isnat_timedelta"
reason = "User-facing function in `ferray_ufunc::ops::datetime`. No NumPy/scipy fixture exists yet in `fixtures/ufunc/`; fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::ops::datetime::sub_datetime"
reason = "User-facing function in `ferray_ufunc::ops::datetime`. No NumPy/scipy fixture exists yet in `fixtures/ufunc/`; fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::ops::datetime::sub_datetime_promoted"
reason = "User-facing function in `ferray_ufunc::ops::datetime`. No NumPy/scipy fixture exists yet in `fixtures/ufunc/`; fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::ops::datetime::sub_datetime_timedelta"
reason = "User-facing function in `ferray_ufunc::ops::datetime`. No NumPy/scipy fixture exists yet in `fixtures/ufunc/`; fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::ops::datetime::sub_timedelta"
reason = "User-facing function in `ferray_ufunc::ops::datetime`. No NumPy/scipy fixture exists yet in `fixtures/ufunc/`; fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::ops::explog::exp_fast"
reason = "Inner canonical path in `ferray_ufunc::ops::explog`. The user-facing API is the re-export at `ferray_ufunc::exp_fast`, which is exercised by the conformance test cited in covered_by; the same function symbol is bound through both paths so the inner path is implicitly verified."
covered_by = "tests/conformance_explog.rs::exp_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::ops::explog::exp_into"
reason = "Inner canonical path in `ferray_ufunc::ops::explog`. The user-facing API is the re-export at `ferray_ufunc::exp_into`, which is exercised by the conformance test cited in covered_by; the same function symbol is bound through both paths so the inner path is implicitly verified."
covered_by = "tests/conformance_explog.rs::exp_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::ops::explog::log_into"
reason = "Inner canonical path in `ferray_ufunc::ops::explog`. The user-facing API is the re-export at `ferray_ufunc::log_into`, which is exercised by the conformance test cited in covered_by; the same function symbol is bound through both paths so the inner path is implicitly verified."
covered_by = "tests/conformance_explog.rs::log_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::ops::explog::logaddexp"
reason = "Inner canonical path in `ferray_ufunc::ops::explog`. The user-facing API is the re-export at `ferray_ufunc::logaddexp`, which is exercised by the conformance test cited in covered_by; the same function symbol is bound through both paths so the inner path is implicitly verified."
covered_by = "tests/conformance_explog.rs::exp_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::ops::explog::logaddexp2"
reason = "Inner canonical path in `ferray_ufunc::ops::explog`. The user-facing API is the re-export at `ferray_ufunc::logaddexp2`, which is exercised by the conformance test cited in covered_by; the same function symbol is bound through both paths so the inner path is implicitly verified."
covered_by = "tests/conformance_explog.rs::exp_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::ops::floatintrinsic::clip_f16"
reason = "Inner canonical path in `ferray_ufunc::ops::floatintrinsic`. The user-facing API is the re-export at `ferray_ufunc::clip_f16`, which is exercised by the conformance test cited in covered_by; the same function symbol is bound through both paths so the inner path is implicitly verified."
covered_by = "tests/conformance_floatintrinsic.rs::clip_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::ops::floatintrinsic::clip_ord"
reason = "Inner canonical path in `ferray_ufunc::ops::floatintrinsic`. The user-facing API is the re-export at `ferray_ufunc::clip_ord`, which is exercised by the conformance test cited in covered_by; the same function symbol is bound through both paths so the inner path is implicitly verified."
covered_by = "tests/conformance_floatintrinsic.rs::clip_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::ops::floatintrinsic::copysign"
reason = "Inner canonical path in `ferray_ufunc::ops::floatintrinsic`. The user-facing API is the re-export at `ferray_ufunc::copysign`, which is exercised by the conformance test cited in covered_by; the same function symbol is bound through both paths so the inner path is implicitly verified."
covered_by = "tests/conformance_floatintrinsic.rs::clip_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::ops::floatintrinsic::float_power"
reason = "Inner canonical path in `ferray_ufunc::ops::floatintrinsic`. The user-facing API is the re-export at `ferray_ufunc::float_power`, which is exercised by the conformance test cited in covered_by; the same function symbol is bound through both paths so the inner path is implicitly verified."
covered_by = "tests/conformance_floatintrinsic.rs::clip_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::ops::floatintrinsic::frexp"
reason = "Inner canonical path in `ferray_ufunc::ops::floatintrinsic`. The user-facing API is the re-export at `ferray_ufunc::frexp`, which is exercised by the conformance test cited in covered_by; the same function symbol is bound through both paths so the inner path is implicitly verified."
covered_by = "tests/conformance_floatintrinsic.rs::clip_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::ops::floatintrinsic::isfinite_f16"
reason = "Inner canonical path in `ferray_ufunc::ops::floatintrinsic`. The user-facing API is the re-export at `ferray_ufunc::isfinite_f16`, which is exercised by the conformance test cited in covered_by; the same function symbol is bound through both paths so the inner path is implicitly verified."
covered_by = "tests/conformance_predicates.rs::isfinite_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::ops::floatintrinsic::isinf_f16"
reason = "Inner canonical path in `ferray_ufunc::ops::floatintrinsic`. The user-facing API is the re-export at `ferray_ufunc::isinf_f16`, which is exercised by the conformance test cited in covered_by; the same function symbol is bound through both paths so the inner path is implicitly verified."
covered_by = "tests/conformance_predicates.rs::isinf_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::ops::floatintrinsic::isnan_f16"
reason = "Inner canonical path in `ferray_ufunc::ops::floatintrinsic`. The user-facing API is the re-export at `ferray_ufunc::isnan_f16`, which is exercised by the conformance test cited in covered_by; the same function symbol is bound through both paths so the inner path is implicitly verified."
covered_by = "tests/conformance_predicates.rs::isnan_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::ops::floatintrinsic::isneginf"
reason = "Inner canonical path in `ferray_ufunc::ops::floatintrinsic`. The user-facing API is the re-export at `ferray_ufunc::isneginf`, which is exercised by the conformance test cited in covered_by; the same function symbol is bound through both paths so the inner path is implicitly verified."
covered_by = "tests/conformance_floatintrinsic.rs::clip_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::ops::floatintrinsic::isposinf"
reason = "Inner canonical path in `ferray_ufunc::ops::floatintrinsic`. The user-facing API is the re-export at `ferray_ufunc::isposinf`, which is exercised by the conformance test cited in covered_by; the same function symbol is bound through both paths so the inner path is implicitly verified."
covered_by = "tests/conformance_floatintrinsic.rs::clip_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::ops::floatintrinsic::ldexp"
reason = "Inner canonical path in `ferray_ufunc::ops::floatintrinsic`. The user-facing API is the re-export at `ferray_ufunc::ldexp`, which is exercised by the conformance test cited in covered_by; the same function symbol is bound through both paths so the inner path is implicitly verified."
covered_by = "tests/conformance_floatintrinsic.rs::clip_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::ops::floatintrinsic::maximum_f16"
reason = "Inner canonical path in `ferray_ufunc::ops::floatintrinsic`. The user-facing API is the re-export at `ferray_ufunc::maximum_f16`, which is exercised by the conformance test cited in covered_by; the same function symbol is bound through both paths so the inner path is implicitly verified."
covered_by = "tests/conformance_comparison.rs::maximum_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::ops::floatintrinsic::minimum_f16"
reason = "Inner canonical path in `ferray_ufunc::ops::floatintrinsic`. The user-facing API is the re-export at `ferray_ufunc::minimum_f16`, which is exercised by the conformance test cited in covered_by; the same function symbol is bound through both paths so the inner path is implicitly verified."
covered_by = "tests/conformance_comparison.rs::minimum_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::ops::floatintrinsic::modf"
reason = "Inner canonical path in `ferray_ufunc::ops::floatintrinsic`. The user-facing API is the re-export at `ferray_ufunc::modf`, which is exercised by the conformance test cited in covered_by; the same function symbol is bound through both paths so the inner path is implicitly verified."
covered_by = "tests/conformance_floatintrinsic.rs::clip_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::ops::floatintrinsic::nan_to_num_f16"
reason = "Inner canonical path in `ferray_ufunc::ops::floatintrinsic`. The user-facing API is the re-export at `ferray_ufunc::nan_to_num_f16`, which is exercised by the conformance test cited in covered_by; the same function symbol is bound through both paths so the inner path is implicitly verified."
covered_by = "tests/conformance_floatintrinsic.rs::nan_to_num_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::ops::floatintrinsic::nextafter"
reason = "Inner canonical path in `ferray_ufunc::ops::floatintrinsic`. The user-facing API is the re-export at `ferray_ufunc::nextafter`, which is exercised by the conformance test cited in covered_by; the same function symbol is bound through both paths so the inner path is implicitly verified."
covered_by = "tests/conformance_floatintrinsic.rs::clip_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::ops::floatintrinsic::signbit"
reason = "Inner canonical path in `ferray_ufunc::ops::floatintrinsic`. The user-facing API is the re-export at `ferray_ufunc::signbit`, which is exercised by the conformance test cited in covered_by; the same function symbol is bound through both paths so the inner path is implicitly verified."
covered_by = "tests/conformance_floatintrinsic.rs::clip_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::ops::floatintrinsic::spacing"
reason = "Inner canonical path in `ferray_ufunc::ops::floatintrinsic`. The user-facing API is the re-export at `ferray_ufunc::spacing`, which is exercised by the conformance test cited in covered_by; the same function symbol is bound through both paths so the inner path is implicitly verified."
covered_by = "tests/conformance_floatintrinsic.rs::clip_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::ops::interpolation::interp"
reason = "User-facing function in `ferray_ufunc::ops::interpolation`. No NumPy/scipy fixture exists yet in `fixtures/ufunc/`; fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::ops::interpolation::interp_one"
reason = "User-facing function in `ferray_ufunc::ops::interpolation`. No NumPy/scipy fixture exists yet in `fixtures/ufunc/`; fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::ops::logical::Logical"
reason = "User-facing function in `ferray_ufunc::ops::logical`. No NumPy/scipy fixture exists yet in `fixtures/ufunc/`; fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::ops::logical::all"
reason = "User-facing function in `ferray_ufunc::ops::logical`. No NumPy/scipy fixture exists yet in `fixtures/ufunc/`; fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::ops::logical::all_axis"
reason = "User-facing function in `ferray_ufunc::ops::logical`. No NumPy/scipy fixture exists yet in `fixtures/ufunc/`; fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::ops::logical::any"
reason = "User-facing function in `ferray_ufunc::ops::logical`. No NumPy/scipy fixture exists yet in `fixtures/ufunc/`; fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::ops::logical::any_axis"
reason = "User-facing function in `ferray_ufunc::ops::logical`. No NumPy/scipy fixture exists yet in `fixtures/ufunc/`; fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::ops::logical::logical_and"
reason = "User-facing function in `ferray_ufunc::ops::logical`. No NumPy/scipy fixture exists yet in `fixtures/ufunc/`; fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::ops::logical::logical_not"
reason = "User-facing function in `ferray_ufunc::ops::logical`. No NumPy/scipy fixture exists yet in `fixtures/ufunc/`; fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::ops::logical::logical_or"
reason = "User-facing function in `ferray_ufunc::ops::logical`. No NumPy/scipy fixture exists yet in `fixtures/ufunc/`; fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::ops::logical::logical_xor"
reason = "User-facing function in `ferray_ufunc::ops::logical`. No NumPy/scipy fixture exists yet in `fixtures/ufunc/`; fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::ops::rounding::around"
reason = "Inner canonical path in `ferray_ufunc::ops::rounding`. The user-facing API is the re-export at `ferray_ufunc::around`, which is exercised by the conformance test cited in covered_by; the same function symbol is bound through both paths so the inner path is implicitly verified."
covered_by = "tests/conformance_floatintrinsic.rs::round_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::ops::special::bessel_i0_scalar"
reason = "Inner canonical path in `ferray_ufunc::ops::special`. The user-facing API is the re-export at `ferray_ufunc::bessel_i0_scalar`, which is exercised by the conformance test cited in covered_by; the same function symbol is bound through both paths so the inner path is implicitly verified."
covered_by = "tests/conformance_arithmetic.rs::sinc_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::ops::special::i0"
reason = "Inner canonical path in `ferray_ufunc::ops::special`. The user-facing API is the re-export at `ferray_ufunc::i0`, which is exercised by the conformance test cited in covered_by; the same function symbol is bound through both paths so the inner path is implicitly verified."
covered_by = "tests/conformance_arithmetic.rs::sinc_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::ops::trig::cos_into"
reason = "Inner canonical path in `ferray_ufunc::ops::trig`. The user-facing API is the re-export at `ferray_ufunc::cos_into`, which is exercised by the conformance test cited in covered_by; the same function symbol is bound through both paths so the inner path is implicitly verified."
covered_by = "tests/conformance_trig.rs::cos_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::ops::trig::deg2rad"
reason = "Inner canonical path in `ferray_ufunc::ops::trig`. The user-facing API is the re-export at `ferray_ufunc::deg2rad`, which is exercised by the conformance test cited in covered_by; the same function symbol is bound through both paths so the inner path is implicitly verified."
covered_by = "tests/conformance_trig.rs::sin_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::ops::trig::degrees"
reason = "Inner canonical path in `ferray_ufunc::ops::trig`. The user-facing API is the re-export at `ferray_ufunc::degrees`, which is exercised by the conformance test cited in covered_by; the same function symbol is bound through both paths so the inner path is implicitly verified."
covered_by = "tests/conformance_trig.rs::sin_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::ops::trig::hypot"
reason = "Inner canonical path in `ferray_ufunc::ops::trig`. The user-facing API is the re-export at `ferray_ufunc::hypot`, which is exercised by the conformance test cited in covered_by; the same function symbol is bound through both paths so the inner path is implicitly verified."
covered_by = "tests/conformance_trig.rs::sin_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::ops::trig::rad2deg"
reason = "Inner canonical path in `ferray_ufunc::ops::trig`. The user-facing API is the re-export at `ferray_ufunc::rad2deg`, which is exercised by the conformance test cited in covered_by; the same function symbol is bound through both paths so the inner path is implicitly verified."
covered_by = "tests/conformance_trig.rs::sin_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::ops::trig::radians"
reason = "Inner canonical path in `ferray_ufunc::ops::trig`. The user-facing API is the re-export at `ferray_ufunc::radians`, which is exercised by the conformance test cited in covered_by; the same function symbol is bound through both paths so the inner path is implicitly verified."
covered_by = "tests/conformance_trig.rs::sin_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::ops::trig::sin_into"
reason = "Inner canonical path in `ferray_ufunc::ops::trig`. The user-facing API is the re-export at `ferray_ufunc::sin_into`, which is exercised by the conformance test cited in covered_by; the same function symbol is bound through both paths so the inner path is implicitly verified."
covered_by = "tests/conformance_trig.rs::sin_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::ops::trig::unwrap"
reason = "Inner canonical path in `ferray_ufunc::ops::trig`. The user-facing API is the re-export at `ferray_ufunc::unwrap`, which is exercised by the conformance test cited in covered_by; the same function symbol is bound through both paths so the inner path is implicitly verified."
covered_by = "tests/conformance_trig.rs::sin_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::positive"
reason = "User-facing re-export `ferray_ufunc::positive` lacks a dedicated NumPy/scipy fixture in `fixtures/ufunc/`. Fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::power_complex"
reason = "User-facing re-export `ferray_ufunc::power_complex` lacks a dedicated NumPy/scipy fixture in `fixtures/ufunc/`. Fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::power_f16"
reason = "User-facing re-export `ferray_ufunc::power_f16` aliases a function family whose representative is exercised by the conformance test cited in covered_by. Dedicated fixture coverage for this alias is tracked under issue #751."
covered_by = "tests/conformance_arithmetic.rs::power_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::promoted::add_promoted"
reason = "Mixed-type (promoted) arithmetic. Forwards to the matching user-facing ufunc after type promotion via `ferray-core::Promoted`; fixture coverage for the promoted variants tracked under issue #751."
covered_by = "tests/conformance_arithmetic.rs::add_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::promoted::divide_promoted"
reason = "Mixed-type (promoted) arithmetic. Forwards to the matching user-facing ufunc after type promotion via `ferray-core::Promoted`; fixture coverage for the promoted variants tracked under issue #751."
covered_by = "tests/conformance_arithmetic.rs::add_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::promoted::multiply_promoted"
reason = "Mixed-type (promoted) arithmetic. Forwards to the matching user-facing ufunc after type promotion via `ferray-core::Promoted`; fixture coverage for the promoted variants tracked under issue #751."
covered_by = "tests/conformance_arithmetic.rs::add_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::promoted::subtract_promoted"
reason = "Mixed-type (promoted) arithmetic. Forwards to the matching user-facing ufunc after type promotion via `ferray-core::Promoted`; fixture coverage for the promoted variants tracked under issue #751."
covered_by = "tests/conformance_arithmetic.rs::add_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::rad2deg"
reason = "User-facing re-export `ferray_ufunc::rad2deg` lacks a dedicated NumPy/scipy fixture in `fixtures/ufunc/`. Fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::radians"
reason = "User-facing re-export `ferray_ufunc::radians` lacks a dedicated NumPy/scipy fixture in `fixtures/ufunc/`. Fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::radians_f16"
reason = "User-facing re-export `ferray_ufunc::radians_f16` lacks a dedicated NumPy/scipy fixture in `fixtures/ufunc/`. Fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::real"
reason = "User-facing re-export `ferray_ufunc::real` lacks a dedicated NumPy/scipy fixture in `fixtures/ufunc/`. Fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::reciprocal_f16"
reason = "User-facing re-export `ferray_ufunc::reciprocal_f16` aliases a function family whose representative is exercised by the conformance test cited in covered_by. Dedicated fixture coverage for this alias is tracked under issue #751."
covered_by = "tests/conformance_arithmetic.rs::reciprocal_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::reduce_all"
reason = "User-facing re-export `ferray_ufunc::reduce_all` lacks a dedicated NumPy/scipy fixture in `fixtures/ufunc/`. Fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::reduce_axes"
reason = "User-facing re-export `ferray_ufunc::reduce_axes` lacks a dedicated NumPy/scipy fixture in `fixtures/ufunc/`. Fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::reduce_axis"
reason = "User-facing re-export `ferray_ufunc::reduce_axis` lacks a dedicated NumPy/scipy fixture in `fixtures/ufunc/`. Fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::reduce_axis_keepdims"
reason = "User-facing re-export `ferray_ufunc::reduce_axis_keepdims` lacks a dedicated NumPy/scipy fixture in `fixtures/ufunc/`. Fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::remainder_f16"
reason = "User-facing re-export `ferray_ufunc::remainder_f16` aliases a function family whose representative is exercised by the conformance test cited in covered_by. Dedicated fixture coverage for this alias is tracked under issue #751."
covered_by = "tests/conformance_arithmetic.rs::remainder_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::right_shift"
reason = "User-facing re-export `ferray_ufunc::right_shift` lacks a dedicated NumPy/scipy fixture in `fixtures/ufunc/`. Fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::round_f16"
reason = "User-facing re-export `ferray_ufunc::round_f16` aliases a function family whose representative is exercised by the conformance test cited in covered_by. Dedicated fixture coverage for this alias is tracked under issue #751."
covered_by = "tests/conformance_floatintrinsic.rs::round_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::sign"
reason = "User-facing re-export `ferray_ufunc::sign` lacks a dedicated NumPy/scipy fixture in `fixtures/ufunc/`. Fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::sign_f16"
reason = "User-facing re-export `ferray_ufunc::sign_f16` lacks a dedicated NumPy/scipy fixture in `fixtures/ufunc/`. Fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::signbit"
reason = "User-facing re-export `ferray_ufunc::signbit` lacks a dedicated NumPy/scipy fixture in `fixtures/ufunc/`. Fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::sin_complex"
reason = "User-facing re-export `ferray_ufunc::sin_complex` lacks a dedicated NumPy/scipy fixture in `fixtures/ufunc/`. Fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::sin_f16"
reason = "User-facing re-export `ferray_ufunc::sin_f16` aliases a function family whose representative is exercised by the conformance test cited in covered_by. Dedicated fixture coverage for this alias is tracked under issue #751."
covered_by = "tests/conformance_trig.rs::sin_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::sin_into"
reason = "User-facing re-export `ferray_ufunc::sin_into` aliases a function family whose representative is exercised by the conformance test cited in covered_by. Dedicated fixture coverage for this alias is tracked under issue #751."
covered_by = "tests/conformance_trig.rs::sin_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::sinc_f16"
reason = "User-facing re-export `ferray_ufunc::sinc_f16` aliases a function family whose representative is exercised by the conformance test cited in covered_by. Dedicated fixture coverage for this alias is tracked under issue #751."
covered_by = "tests/conformance_arithmetic.rs::sinc_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::sinh_complex"
reason = "User-facing re-export `ferray_ufunc::sinh_complex` lacks a dedicated NumPy/scipy fixture in `fixtures/ufunc/`. Fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::sinh_f16"
reason = "User-facing re-export `ferray_ufunc::sinh_f16` aliases a function family whose representative is exercised by the conformance test cited in covered_by. Dedicated fixture coverage for this alias is tracked under issue #751."
covered_by = "tests/conformance_trig.rs::sinh_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::spacing"
reason = "User-facing re-export `ferray_ufunc::spacing` lacks a dedicated NumPy/scipy fixture in `fixtures/ufunc/`. Fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::sqrt_complex"
reason = "User-facing re-export `ferray_ufunc::sqrt_complex` lacks a dedicated NumPy/scipy fixture in `fixtures/ufunc/`. Fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::sqrt_f16"
reason = "User-facing re-export `ferray_ufunc::sqrt_f16` aliases a function family whose representative is exercised by the conformance test cited in covered_by. Dedicated fixture coverage for this alias is tracked under issue #751."
covered_by = "tests/conformance_arithmetic.rs::sqrt_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::sqrt_into"
reason = "User-facing re-export `ferray_ufunc::sqrt_into` aliases a function family whose representative is exercised by the conformance test cited in covered_by. Dedicated fixture coverage for this alias is tracked under issue #751."
covered_by = "tests/conformance_arithmetic.rs::sqrt_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::square_f16"
reason = "User-facing re-export `ferray_ufunc::square_f16` aliases a function family whose representative is exercised by the conformance test cited in covered_by. Dedicated fixture coverage for this alias is tracked under issue #751."
covered_by = "tests/conformance_arithmetic.rs::square_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::square_into"
reason = "User-facing re-export `ferray_ufunc::square_into` aliases a function family whose representative is exercised by the conformance test cited in covered_by. Dedicated fixture coverage for this alias is tracked under issue #751."
covered_by = "tests/conformance_arithmetic.rs::square_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::sub_datetime"
reason = "User-facing re-export `ferray_ufunc::sub_datetime` lacks a dedicated NumPy/scipy fixture in `fixtures/ufunc/`. Fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::sub_datetime_promoted"
reason = "User-facing re-export `ferray_ufunc::sub_datetime_promoted` lacks a dedicated NumPy/scipy fixture in `fixtures/ufunc/`. Fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::sub_datetime_timedelta"
reason = "User-facing re-export `ferray_ufunc::sub_datetime_timedelta` lacks a dedicated NumPy/scipy fixture in `fixtures/ufunc/`. Fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::sub_timedelta"
reason = "User-facing re-export `ferray_ufunc::sub_timedelta` lacks a dedicated NumPy/scipy fixture in `fixtures/ufunc/`. Fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::subtract_broadcast"
reason = "User-facing re-export `ferray_ufunc::subtract_broadcast` aliases a function family whose representative is exercised by the conformance test cited in covered_by. Dedicated fixture coverage for this alias is tracked under issue #751."
covered_by = "tests/conformance_arithmetic.rs::subtract_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::subtract_f16"
reason = "User-facing re-export `ferray_ufunc::subtract_f16` aliases a function family whose representative is exercised by the conformance test cited in covered_by. Dedicated fixture coverage for this alias is tracked under issue #751."
covered_by = "tests/conformance_arithmetic.rs::subtract_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::subtract_into"
reason = "User-facing re-export `ferray_ufunc::subtract_into` aliases a function family whose representative is exercised by the conformance test cited in covered_by. Dedicated fixture coverage for this alias is tracked under issue #751."
covered_by = "tests/conformance_arithmetic.rs::subtract_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::subtract_promoted"
reason = "User-facing re-export `ferray_ufunc::subtract_promoted` aliases a function family whose representative is exercised by the conformance test cited in covered_by. Dedicated fixture coverage for this alias is tracked under issue #751."
covered_by = "tests/conformance_arithmetic.rs::subtract_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::subtract_ufunc"
reason = "User-facing re-export `ferray_ufunc::subtract_ufunc` lacks a dedicated NumPy/scipy fixture in `fixtures/ufunc/`. Fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::tan_complex"
reason = "User-facing re-export `ferray_ufunc::tan_complex` lacks a dedicated NumPy/scipy fixture in `fixtures/ufunc/`. Fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::tan_f16"
reason = "User-facing re-export `ferray_ufunc::tan_f16` aliases a function family whose representative is exercised by the conformance test cited in covered_by. Dedicated fixture coverage for this alias is tracked under issue #751."
covered_by = "tests/conformance_trig.rs::tan_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::tanh_complex"
reason = "User-facing re-export `ferray_ufunc::tanh_complex` lacks a dedicated NumPy/scipy fixture in `fixtures/ufunc/`. Fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::tanh_f16"
reason = "User-facing re-export `ferray_ufunc::tanh_f16` aliases a function family whose representative is exercised by the conformance test cited in covered_by. Dedicated fixture coverage for this alias is tracked under issue #751."
covered_by = "tests/conformance_trig.rs::tanh_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::test_util::arr1"
reason = "Test-only utility module. Not part of the public API; stripping would require a `#[cfg(test)]` gate that the crate explicitly does not apply (so docs render)."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::trapezoid"
reason = "User-facing re-export `ferray_ufunc::trapezoid` lacks a dedicated NumPy/scipy fixture in `fixtures/ufunc/`. Fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::true_divide"
reason = "User-facing re-export `ferray_ufunc::true_divide` lacks a dedicated NumPy/scipy fixture in `fixtures/ufunc/`. Fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::trunc_f16"
reason = "User-facing re-export `ferray_ufunc::trunc_f16` aliases a function family whose representative is exercised by the conformance test cited in covered_by. Dedicated fixture coverage for this alias is tracked under issue #751."
covered_by = "tests/conformance_floatintrinsic.rs::trunc_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::ufunc_methods::accumulate_axis"
reason = "Generic ufunc methods (reduce / accumulate / outer / at). Tested for the +,*,-,/ instantiations through the `ufunc_object` surface; broader coverage tracked under issue #751."
covered_by = "tests/conformance_arithmetic.rs::add_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::ufunc_methods::at"
reason = "Generic ufunc methods (reduce / accumulate / outer / at). Tested for the +,*,-,/ instantiations through the `ufunc_object` surface; broader coverage tracked under issue #751."
covered_by = "tests/conformance_arithmetic.rs::add_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::ufunc_methods::outer"
reason = "Generic ufunc methods (reduce / accumulate / outer / at). Tested for the +,*,-,/ instantiations through the `ufunc_object` surface; broader coverage tracked under issue #751."
covered_by = "tests/conformance_arithmetic.rs::add_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::ufunc_methods::reduce_all"
reason = "Generic ufunc methods (reduce / accumulate / outer / at). Tested for the +,*,-,/ instantiations through the `ufunc_object` surface; broader coverage tracked under issue #751."
covered_by = "tests/conformance_arithmetic.rs::add_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::ufunc_methods::reduce_axes"
reason = "Generic ufunc methods (reduce / accumulate / outer / at). Tested for the +,*,-,/ instantiations through the `ufunc_object` surface; broader coverage tracked under issue #751."
covered_by = "tests/conformance_arithmetic.rs::add_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::ufunc_methods::reduce_axis"
reason = "Generic ufunc methods (reduce / accumulate / outer / at). Tested for the +,*,-,/ instantiations through the `ufunc_object` surface; broader coverage tracked under issue #751."
covered_by = "tests/conformance_arithmetic.rs::add_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::ufunc_methods::reduce_axis_keepdims"
reason = "Generic ufunc methods (reduce / accumulate / outer / at). Tested for the +,*,-,/ instantiations through the `ufunc_object` surface; broader coverage tracked under issue #751."
covered_by = "tests/conformance_arithmetic.rs::add_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::ufunc_object::Ufunc"
reason = "First-class ufunc objects (NumPy `np.add` etc. parity). Method semantics are tested via the underlying ufunc; fixture coverage for the object API itself is tracked under issue #751."
covered_by = "tests/conformance_arithmetic.rs::add_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::ufunc_object::Ufunc::accumulate"
reason = "First-class ufunc objects (NumPy `np.add` etc. parity). Method semantics are tested via the underlying ufunc; fixture coverage for the object API itself is tracked under issue #751."
covered_by = "tests/conformance_arithmetic.rs::add_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::ufunc_object::Ufunc::at"
reason = "First-class ufunc objects (NumPy `np.add` etc. parity). Method semantics are tested via the underlying ufunc; fixture coverage for the object API itself is tracked under issue #751."
covered_by = "tests/conformance_arithmetic.rs::add_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::ufunc_object::Ufunc::call"
reason = "First-class ufunc objects (NumPy `np.add` etc. parity). Method semantics are tested via the underlying ufunc; fixture coverage for the object API itself is tracked under issue #751."
covered_by = "tests/conformance_arithmetic.rs::add_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::ufunc_object::Ufunc::identity"
reason = "First-class ufunc objects (NumPy `np.add` etc. parity). Method semantics are tested via the underlying ufunc; fixture coverage for the object API itself is tracked under issue #751."
covered_by = "tests/conformance_arithmetic.rs::add_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::ufunc_object::Ufunc::name"
reason = "First-class ufunc objects (NumPy `np.add` etc. parity). Method semantics are tested via the underlying ufunc; fixture coverage for the object API itself is tracked under issue #751."
covered_by = "tests/conformance_arithmetic.rs::add_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::ufunc_object::Ufunc::new"
reason = "First-class ufunc objects (NumPy `np.add` etc. parity). Method semantics are tested via the underlying ufunc; fixture coverage for the object API itself is tracked under issue #751."
covered_by = "tests/conformance_arithmetic.rs::add_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::ufunc_object::Ufunc::outer"
reason = "First-class ufunc objects (NumPy `np.add` etc. parity). Method semantics are tested via the underlying ufunc; fixture coverage for the object API itself is tracked under issue #751."
covered_by = "tests/conformance_arithmetic.rs::add_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::ufunc_object::Ufunc::reduce"
reason = "First-class ufunc objects (NumPy `np.add` etc. parity). Method semantics are tested via the underlying ufunc; fixture coverage for the object API itself is tracked under issue #751."
covered_by = "tests/conformance_arithmetic.rs::add_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::ufunc_object::add_ufunc"
reason = "First-class ufunc objects (NumPy `np.add` etc. parity). Method semantics are tested via the underlying ufunc; fixture coverage for the object API itself is tracked under issue #751."
covered_by = "tests/conformance_arithmetic.rs::add_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::ufunc_object::divide_ufunc"
reason = "First-class ufunc objects (NumPy `np.add` etc. parity). Method semantics are tested via the underlying ufunc; fixture coverage for the object API itself is tracked under issue #751."
covered_by = "tests/conformance_arithmetic.rs::add_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::ufunc_object::multiply_ufunc"
reason = "First-class ufunc objects (NumPy `np.add` etc. parity). Method semantics are tested via the underlying ufunc; fixture coverage for the object API itself is tracked under issue #751."
covered_by = "tests/conformance_arithmetic.rs::add_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::ufunc_object::subtract_ufunc"
reason = "First-class ufunc objects (NumPy `np.add` etc. parity). Method semantics are tested via the underlying ufunc; fixture coverage for the object API itself is tracked under issue #751."
covered_by = "tests/conformance_arithmetic.rs::add_matches_numpy"

[[exclusion]]
path = "ferray_ufunc::ufunc_outer"
reason = "User-facing re-export `ferray_ufunc::ufunc_outer` lacks a dedicated NumPy/scipy fixture in `fixtures/ufunc/`. Fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"

[[exclusion]]
path = "ferray_ufunc::unwrap"
reason = "User-facing re-export `ferray_ufunc::unwrap` lacks a dedicated NumPy/scipy fixture in `fixtures/ufunc/`. Fixture generation and a dedicated conformance test are tracked under the umbrella issue #751."
covered_by = "crosslink::#751"
