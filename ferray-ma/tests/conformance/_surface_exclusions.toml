# ferray-ma surface exclusions (Stage 4-ma).
#
# Items in this file are public surface paths that the
# strict surface-coverage gate does NOT find a literal text
# reference for in any tests/conformance_*.rs file, but which
# are documented elsewhere (in-crate unit tests, downstream
# callers, or the umbrella tracking issue #754).
#
# DO NOT add generic exclusions. Each entry must cite where
# the behaviour is actually validated (`covered_by`).
