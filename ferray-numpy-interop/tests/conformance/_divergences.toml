# Documented cases where ferray-numpy-interop returns a more mathematically
# correct value than the reference. None currently documented for
# ferray-numpy-interop (this crate handles type conversion, not arithmetic).
